# Sync Note — 8 Sep 2026: Mission Rework pass (all 72 missions)

*Why this exists: `Bloom_Wars_History.md` has no copy on Maxime's device to stage, and retyping it whole through `project_write` is the silent-drift risk Foundation's working rules exist to prevent. `Bloom_Wars_Now_And_Next.md` WAS updated this session (its edits were applied through exact-match anchors on a local copy, every anchor asserted once — the eleventh-pass note at its top, "Where the game is," four new "Needs Maxime's call" items, the "Yours specifically" playtest bullet, the "More spawns" bullet, one "Needs verifying" bullet). This note carries the History entry, ready to append with an exact-match edit by the next session that has the file on disk. The addendum is the authority either way.*

## For `Bloom_Wars_History.md` — one new entry, append at the end

> ## Mission rework, all 72 missions — 8 September 2026
>
> Maxime's brief, verbatim: *"there 72 mission, the goal is to make them hard for the player... you have carte blanche to rework the map the spawn, the spwn point and objective area, the rest is pretty much struck in concreet. hard ai should only succeed less than 10%, but honestly dont ship me a mission you havent tested and rated... go until you run out of credit. then do me a rapport."* The finding that reframed the pass: every balance number the project ever produced was measured with an all-G-tier squad, and against the project's own tier schedule (a new `--progression` harness flag) the shipped campaign was 93.7% mean Hard win, 67 of 72 at 80% or better. What shipped: every mission respecified in one data file (`design/mission_rework/mission_rework_specs.py`, applied mechanically to both campaign TS files — the spec is now the source of truth for waves the way `design/maps_*.py` is for maps); 15 maps changed through the generators; ten bonus objectives (five new); one small engine hook (hostile-mech waves and spawn events carry a tier, `createHostileMechUnit(…, tierOverride)`, 4 tests); five test-bot fixes (hold-zone VIP guard and deadline walk, hold-zone clear commit, escort discipline, cork priority); `showMap.ts` and `endState.ts`. Result at n=50 Hard: mean 30.7%, median 23%, 20 missions at or under 10%, at four mechs down and 0.75 permanent losses per run. Wire and Mud / The Long Contract map widenings were reverted — both briefings promise one door and `mapsAmaranth.test.ts` guards it. **107 files / 2587 tests**, `tsc`/eslint clean, `vite build` not run (spoiler lint needs the local `.env.local`). 33 files committed, zero rejections. **Nothing played live.** Full account, the 72-row table, the difficulty policy, the four honest limits (corked extracts the bot won't walk into, commander focus-fire, Act III Bloom scaling, the hold-zone throughput cap) and the ten-mission playtest list: `claude/Bloom_Wars_Build_Log_Addendum_MissionRework_08Sep2026.md`.
>
> **Genuinely still open:** the ten missions the harness cannot rate (W5, W7, W11, W17, W21, HA3, HA6-8, HA26, HA28) want Maxime's hands; four calls in the addendum's §9 (Act I vs onboarding, Bloom scaling for Act III, two story flags, whether commander focus-fire stays "by design"); and every GDD/Data Pack mission table is stale against the spec file.

## Docs that went stale this pass (also listed in the addendum §10)

GDD / Data Pack mission tables; `Bloom_Wars_Player_AI_Difficulty_Tiers_Plan_v1`; the EA Launch Plan balance line; `Bloom_Wars_House_Amaranth_Full_Campaign_Plan_v1` §6; `design/build_log` per-mission mds; the Undertow Ambush Hold scoping doc's all-G measurement. `Bloom_Wars_Now_And_Next.md` is current. The in-progress handoff (`Bloom_Wars_Mission_Rework_INPROGRESS_Handoff_08Sep2026.md`) was deleted once the addendum existed.
