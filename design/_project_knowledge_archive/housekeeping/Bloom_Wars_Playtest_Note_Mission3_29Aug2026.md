# Playtest note — Mission 3, flagged 29 Aug 2026

**Not investigated, not fixed — just captured. Maxime: "tally only plz until I say built it."**

Maxime, playtesting Mission 3 himself: the mission can currently be completed without engaging a single Bloom unit — nothing forces a real fight.

**His own proposed fix, not built:** spawn the Bloom in "the patch" (an in-map Bloom-growth feature) at the eastern edge, positioned so the Bloom itself is driven to try to clear that zone — which would force the player to actively defend their Muntis there rather than being able to just route around the fight entirely.

Worth checking later, not now: Mission 3's actual map/spawn data and objective type against this reading (per this project's own rule — a map or balance number only counts once it's run through `maps.py`/`combat_sim.py`), and whether "the patch" already exists as a named map feature or would be new map content.
