# THE BLOOM WARS — The Antfarm: Carrier Hub & Between-Mission Story Layer v1

**A design pass turning two things into one thing — NON-CANON where it touches the Amaranth Reckoning, additive everywhere else**

*Written 22 August 2026. Answers two asks that turned out to be the same feature: "add Sunrider-style story moments between missions" and "start planning the antfarm carrier — workshop, heirloom, energy, more squads, faster ship fires." Both point at the same screen. This document builds that screen.*

**Update, same day, after the permadeath/recruit-phase design landed.** That design (full reasoning in `claude/Bloom_Wars_Spitball_Ideas.md`, practical summary in the campaign doc's new §6a) needs a real UI home starting Mission 1 — the deploy gate and the recruit tracks aren't Act-II-only, they're live from Muster onward. That collides gently with this doc's own §9 recommendation ("Tier 1 lands alongside Act II's prototype... not before Act I is proven") and its §1 claim that "Act I has no hub." Reconciled in the new §6a below rather than silently changed: the *engine and data* this system needs are being built now, starting with Act I, because they have to be; the *Antfarm identity* — Providence, the room fiction, the banter — still waits for Act II exactly as originally planned. Act I gets a plain, unbranded meta-screen shaped like the future rooms; Act II is where it becomes the Antfarm for real.

**Update, 23 August 2026 (cont'd) — five of §11.2's open questions resolved.** Maxime's calls, verbatim where it matters: Weapons Bay "reduce cd on 14 weapon by half" (flat effect, not a menu — the ability itself still unlocks at Mission 14 exactly as §1 already had it); Generator "allow you to power fabricator and other heavy room" (the heavier, genre-faithful option — a real power budget, not just a bigger Energy cap); Beacon and Restock Room "yes separate," Restock Room's job specifically to "reduce the after effect of a restock"; and stress/willpower gets a real shape even though it's not being fully designed yet — see the new §11.4. Rolled into the §11.2 table and a new §11.4 below rather than rewritten in place, same as this doc's other updates. Maxime's framing: "an XCOM type ant farm... we are going to add new room to it as I think of them," buildable "as soon as they can in act 1, except for arc specific locked thing." This turns §3's Carrier Upgrade Modules from an abstract points-spend with no spatial representation into what he actually pictured — a literal build grid, twelve bays around a new, sixth fixed compartment (the CO's grotto) that isn't one of §2's five narrative rooms and isn't one of the twelve either. Full pass in the new §11. Doesn't override the Tier 0/Tier 1 split above: bay *construction* is real starting Act I (Tier 0/1 territory, arc-locked bays excepted, same as Forward Battery already being unbuyable before Mission 14), the Providence/Antfarm *skin* still waits for Act II exactly as already decided.

**Update, 23 August 2026 (cont'd) — the hub itself moves up to Act I.** Maxime: "lets make the ant farm something you can build as soon as act 1. but your gated on space and type of room until you get promoted... its the social hub so its kinds impirtant." This overturns §1's original "Act I has no hub" call — not the Tier 0 recruit-phase engine work, which was always live from Mission 1 regardless (§6a), but the *narrative* hub itself: Hangar Deck, Berths, and the rest, plus every `HubScene` beat that depends on them (§8's own beat table already scheduled one after Mission 1, which was quietly inconsistent with "no hub" from the day it was written). The reasoning is direct: this is the social/story layer, and holding it back for a third of the campaign costs more than it protects. Resolved with a rank-gated build system rather than a flat mission-number unlock — full design in the new §12. §1's original bullet is marked superseded rather than deleted, since the reasoning it gave (Providence isn't forward-deployed) still needed an answer, not just an override — §12 gives it one.

**Update, 23 August 2026 (cont'd) — what the social layer is actually for.** Maxime laid out what he pictures the hub *doing*, not just when it unlocks: "I want the hub to be the social spot like the Normandy was. in it player should be allowed to talk to the crew, the co once that open up and help alleviate stress, romance other pilot, eat shit because you let someone thst got romance die. get drunk at the rec room. stuff like that." Then: "look at spitballing idea we should have more on this." That content lives in `claude/Bloom_Wars_Spitball_Ideas.md` (its "Sunrider-style social layer" entry, same date) rather than duplicated here, since that's the project's dedicated pre-canon parking lot and this doc is already long — but it touches enough of this doc's own sections (the grotto, Stress, Berths) that short pointers are dropped at each one below so nothing here goes stale next to it.

---

## 0. Two asks, one answer

The Spitball doc already parked this, twice, without connecting the two entries: "Carrier as an ant-base hub, XCOM Avenger style — a real base-management layer between missions" sits a few lines above "Sunrider's EN-budget system... stays parked for a possible future ship layer." They're the same future. A hub screen is exactly where XCOM's Avenger-walk banter lives *and* exactly where Sunrider's ship-deck conversations live — those are two well-known instances of one genre convention, not two features. Build one hub. It carries the workshop, the Heirloom Vault, the Energy resource, roster growth, and the social scenes, because a real place has rooms and rooms are where all of this already wants to live.

This does not replace the existing debrief screen's mechanics (gear tier purchases, spare mek parts, mek secondaries — Build Brief step 11, GDD §6.4). It gives that screen a floor plan and adds new things to spend on and look at once you're standing in it.

**Scope note, matching the Amaranth Reckoning doc's own discipline:** this hub is designed for a battalion-scale campaign — a roster that grows, a ship that can be damaged, veterans who earn named ultimates. The 4-mission vertical slice (5 pilots, no composition choice, one shared Heirloom slot) doesn't need it and nothing here touches that slice. Amaranth Reckoning is the first campaign big enough to justify building this, which is presumably why the ask landed here first.

## 1. The identity call: Providence is the Antfarm

Recommendation, easy to veto: the hub *is* Providence, the ship Amaranth Reckoning already names for Act II fire support (Mission 14, "Steel Rain") and already puts in harm's way (Mission 22, "Ash on the Water" — "the Providence takes real damage"). Rather than inventing a fourth named ship, give an existing one a second job. The crew nickname for it is "the Antfarm" — not its service designation, just what Warden Company calls the thing they keep tunneling new rooms into every time the roster outgrows it, which is often. That's an in-fiction joke that also happens to explain the mechanic: the hub keeps getting bigger because the war keeps making it need to.

Consequences this creates, all of them already consistent with the existing table in §10 of the campaign doc:

- **Act I has no hub — superseded, 23 Aug 2026, see §12.** This bullet originally held that Providence isn't forward-deployed until Mission 14, so the hub couldn't exist before then either. Untangled instead of overridden: Providence is present as Warden Company's rear logistics support from Act I onward (a WW1-style trench-war lance holding a border line plausibly has *some* rear support from day one) — only its **fire-support call-in ability** stays gated to Mission 14 exactly as already locked; "off-board support: none" in the squad-scaling table still means no call-ins, not no ship. The hub's existence and the ship's weapon are two different unlocks, always were — this just says so explicitly. §12 has the real gating: rank, not mission number.
- **Mission 22 gets real teeth.** "Protect Asset" currently means an off-board ship with a damage state. If that ship is also the player's own hub, damage to it can knock a room or an upgrade offline for a mission or two — the first time the player's *base* is the thing at risk, rehearsing exactly the stakes Act III needs before Act III asks for them. The doc already calls this mission a "rehearsal for Act III's capital-ship stakes"; this makes it rehearse the right thing.
- **Meridian's Oath is untouched.** That's a separate capital ship introduced in Act III (Mission 25) for capital-scale fire support and Mission 32's defense — a different, bigger, later thing. Nothing here renames or merges it with Providence.

**The one thing this doc has to protect:** Providence stays scenery. It is never piloted, never a played unit, never a ship-combat layer — the hub is a menu you visit between missions, not a mode you play. That's the same discipline the campaign doc already locked for the capital-ship objective missions ("the ship is fragile scenery you defend, never a unit you pilot, keeping the 'Freespace never gave us capital ships' instinct intact"). A hub with rooms in it is a much shorter step toward accidentally building ship combat than it looks like from here — worth a flag now rather than a scope argument in six months.

## 2. The rooms

Five rooms for v1. Each does one mechanical job and one narrative job — that pairing is deliberate, not incidental, because it's what keeps this from being a menu with flavor text bolted on.

| Room | Mechanical job | Narrative job |
| --- | --- | --- |
| **Hangar Deck** | Roster and deploy management, mission select, **the Munti deploy gate (new — §6a)** | Low-stakes group banter — the room everyone passes through, so it's the room where casual voice lives |
| **The Workshop** | Gear tier purchases, mek secondaries, spare parts, carrier upgrade modules (new — §3, **now including three recruit-phase modules — §6a**) | The Quartermaster's domain; running commentary on the war through its logistics, not its battles |
| **The Vault** | Heirloom management, dedication, salvage integration (new — §4) | Grief-adjacent. The campaign's heaviest room, used sparingly on purpose |
| **Berths** | Lance recruitment, roster capacity (new — §6), **now the literal home of the recruit-phase system — §6a** | One-on-one pilot scenes — the actual Sunrider slot: quarters, rest, the people rather than the war |
| **CIC / Bridge** | Fire-support call-in configuration, Energy allocation (new — §5) | Marrow sightings, House Amaranth politics, campaign-plot beats — briefing-adjacent in subject, never in tone |

**Cross-ref, 23 Aug 2026:** Berths' "one-on-one pilot scenes" job now has real texture on record — `claude/Bloom_Wars_Spitball_Ideas.md`'s expanded social-layer entry (same date) parks a romance mechanic there, plus its own grief consequence when a romanced pilot is permanently lost. Not designed here, not built — see that doc for the actual reasoning.

A sixth room, a common mess deck with no mechanical function at all, is worth wanting and worth not building yet — see §9. **Partly un-parked by §11's Rec Room — see there.**

## 3. The Workshop (added workshop)

Two layers. The first already exists and just moves into this room unchanged: gear tier purchases, spare mek parts, mek secondary specializations, at their existing costs (Data Pack §12.1). The second is new.

**Carrier Upgrade Modules** — a second thing the same points currency buys, spent from the ship's own ledger rather than a pilot's. This is a deliberate expansion of "points buy exactly three things, which is deliberately few" (GDD §6.4), and it's licensed by the campaign doc's own Appendix B, which already flags that the points economy "needs its own balance pass at build time" once it's scaled across 36 missions and a fivefold roster — this is that scaling, not a new precedent. Every module carries a real cost, same rule the Heirloom table already enforces: nothing here is strictly better than not buying it. **As of §11, these seven are bays in the physical build grid, not a separate system — same costs, same effects, just a place now.**

| Module | Effect | Cost |
| --- | --- | --- |
| **Auxiliary Berths** | Pulls a lance's worth of deploy slots in early, ahead of the Act's scripted schedule ("more squads," on demand rather than only at Act breaks) | Heavy point cost, and a permanent DEF penalty to Providence itself for the rest of the act — crew diverted to billeting is crew not on damage-control stations, which is exactly what Mission 22 is waiting to punish |
| **Forward Battery** | **Resolved, 23 Aug 2026 — no longer a standalone lever.** Now an *upgrade tier built on top of* §11.2's Weapons Bay, not a separate/competing cooldown module. Weapons Bay cuts the Mission-14 weapon's cooldown 50% on its own; Forward Battery adds another 25% on top, for 75% total cooldown reduction — final cooldown sits at 25% of baseline, not 75% (written out plainly since "75% total" reads two ways). Maxime: "foward battery should be a upgrade to weapon bay and reduce cooldown by 25% for a 75%, total cd." | Heavy point cost, not purchasable before Mission 14 and now also gated behind Weapons Bay being built first. **Dropped, not carried forward:** the old "or reduces Energy cost" clause — this description only covers cooldown, so treating the Energy-cost lever as retired unless told otherwise; it doesn't have a home right now. |
| **Fabrication Bay Expansion** | Raises every Fabricator mek's spare-part cap campaign-wide, not just one pilot's | Point cost scaled to how many Fabricator meks are in the current roster, so it gets more expensive as the payoff gets bigger |
| **Runic Integration Line** | Required before a *salvaged* Heirloom (see §4 — The Debutante's Answer is the one example that exists today) can be assigned to anyone. Without it, a salvaged Heirloom sits in the Vault, inert | Moderate point cost, one-time |
| **Combat Medic Cadre** (new, 22 Aug 2026) | Discretionary-track Munti recruits (§6a) enter at F-tier instead of G — a reward for a player who's identified Munti as the load-bearing class and is deliberately building depth there, not a change to the free emergency replacement, which stays bare-bones on purpose | Moderate point cost, one-time |
| **Reserve Muster** (new, 22 Aug 2026) | A discretionary recruit (any class, bought proactively — §6a) is available immediately instead of waiting for the next debrief. Speed, not power; still doesn't touch the emergency track | Moderate point cost, one-time |
| **Vital Signs Uplink** (new, 22 Aug 2026) | Early-warning HUD cue when a side's last living Munti drops below a set HP threshold mid-mission — advance notice instead of only learning the safety net is off after it's already off | Moderate point cost, one-time |

## 4. The Vault (added heirloom)

A physical home for the 20-name Heirloom pool the campaign doc already built (§9 of the Amaranth Reckoning doc). Nothing about the pool's rules changes; the Vault is where the rule everyone already agreed to becomes a scene instead of a stat change.

The existing unlock condition — reaching A tier is "Heirloom-adjacent," and *surviving a real story beat afterward* is the actual unlock, "not a shop purchase" — currently has no "where." The Vault is the where. A veteran's dedication happens here: a short scene, once, the moment the story beat resolves. Requiem's transfer from Bosk to Rourke at the end of Mission 12 is the obvious first one to write, and it should be treated as load-bearing the way the GDD uses that exact word for the base slice's Mission 1b briefing — "the only thing the game ever says about a mission whose weight the player is expected to work out on their own." Same idea, moved from a briefing to a scene: this is the one moment in the whole campaign the hub isn't allowed to be quiet about. See §7 below for why it doesn't get to be skippable. **Now genuinely conditional rather than certain — per §6a of the campaign doc, this scene only happens exactly as written if Bosk is actually the one who reaches Mission 12. If he isn't, this scene needs a version that names whoever actually did.**

Salvaged Heirlooms are a second, smaller case worth naming explicitly: The Debutante's Answer is tagged "salvaged, not issued" in the existing table, which already implies it doesn't arrive through the normal unlock path. The Vault is where it sits, uncalibrated, until the Workshop's Runic Integration Line is bought — a small, concrete link between the two rooms rather than a special case bolted onto one ability.

**Open, not decided here:** whether a permanently lost pilot's Heirloom (if they were carrying one) is retired, orphaned back into the pool, or memorialized in the Vault as unusable. The campaign doc doesn't currently say whether Amaranth Reckoning reuses the base game's "points invested in a lost pilot are not recovered" rule (Canon Pass §C.3) for Heirlooms specifically. **Sharper now that permadeath is a real, general system rather than a single scripted Mission 12 loss — this will come up more than once across a campaign, worth resolving before the Vault actually gets built rather than after.**

## 5. Energy (added energy)

A resource that belongs to the ship, not to any pilot or mek — a separate axis from HP, points, or Heirloom charge, the same way the campaign doc's own research already scoped it: Sunrider's EN-budget was explicitly researched, explicitly rejected for the mech-combat layer (two-action XCOM economy won that argument on its merits), and explicitly "parked for a possible future ship layer instead." This is that ship layer, finally given something to be attached to.

- **What it fuels:** Providence's fire-support call-ins, and — if this ever gets built out — active defense during Protect Asset missions (Ash on the Water, Hold at the Spire), which right now are pure damage-sponge scenery with no player lever. Energy is a cheap way to give the player one button in those missions without turning them into ship combat.
- **How it's gained:** flat regen per mission completed, the same performance bonuses already defined for points (Data Pack §12.3 — turns under limit, no pilot downed, no spare parts spent) also feed Energy. No new tracked stats, no new scoring rules — the existing scorecard just has a second column now. **§11's Generator bay is a second lever on this — see there.**
- **Cap and use-it-or-lose-it:** capped per act, and unused Energy does not carry across an act boundary. That's a deliberate contrast with points (which persist) — it keeps each act's Energy budget a real decision made on that act's own terms, rather than something that snowballs into Act III being trivially funded by Act I discipline.
- **Its cost is logistical, not personal.** Severance's whole design point is that its cost is felt immediately and specifically — it can kill your own unit, right now, on purpose. Energy shouldn't try to copy that; it's a different tension serving a different scale. Its cost is opportunity and scarcity: what you didn't buy this act because you spent Energy instead of banking it. That's a real cost. It doesn't need to also be a dramatic one — the game already has a mechanic that does dramatic, and diluting Severance's exclusivity on "this can hurt you" would cost more than it'd add.

## 6. Berths and squad growth (more squads)

The campaign doc already scripts roster growth — 5 at Act I, 10 across two lances at Act II open, roughly 20 across four lances by Act III (§10's squad-scaling table). That schedule stays the floor; nothing here delays or replaces it. Auxiliary Berths (§3) is the only lever that moves it, and only earlier, and only at the DEF cost already described. This keeps the campaign's pacing author-controlled by default, with hub investment as optional acceleration for a player who wants to pay for it — additive, not a rewrite of a table that's already been thought through.

## 6a. The recruit-phase system lives here first — before the hub does (new, 22 Aug 2026)

Full design and every direct quote preserved in `claude/Bloom_Wars_Spitball_Ideas.md`; practical summary in the campaign doc's §6a. This section is just the room-wiring and the Act I sequencing fix.

**Where it lives, mechanically, once the hub exists:** the deploy gate (no mission launches without a living Munti in the squad) is Hangar Deck's job, same room that already owns roster/deploy management. The two recruit tracks — automatic free emergency replacement the moment living Munti count hits zero, and paid discretionary recruiting for anyone buying a backup proactively — are Berths' job, the room that already owned "lance recruitment, roster capacity" before this system existed. Nothing here needed a new room; the existing five already had the right shape.

**Where it lives *before* the hub exists — the actual gap this update closes.** The recruit system isn't an Act II feature. It has to work starting Mission 1, because the deploy gate and the permadeath rule are live from Muster onward, and §1's "Act I has no hub" is still correct as fiction — Providence isn't forward-deployed yet, nothing here changes that. The resolution: build the underlying state and rules now, as plain engine/data work with no room fiction attached, and let Act I use an unbranded functional meta-screen (a squad-select/debrief screen that does the Hangar-Deck and Berths jobs without being called that, without Providence, without banter) until Mission 14 unlocks the real Antfarm and that same functionality gets the room skin and the scenes on top. Concretely: the same underlying functions and state (`checkMuntiGuarantee`, permanent-loss tracking, the emergency/discretionary recruit paths) get called from a bare Act I screen first and a fully-dressed Hangar Deck / Berths later — one system, two skins, exactly the same pattern §9 of this doc already uses for CO powers and Heirloom effects elsewhere in the project. Nothing about this weakens the "Tier 1 lands alongside Act II" build recommendation for the *narrative* hub — it only means the *rules* can't wait that long, because Mission 1 needs them to be true. **§11's grotto is a second thing that now needs the same two-skins treatment — see there.** **Superseded in one respect, 23 Aug 2026 — see §12: the narrative hub no longer waits for Act II either, so Hangar Deck and Berths specifically get their room skin much sooner than this paragraph originally planned. The "two skins, one system" pattern itself is untouched — only the calendar for when the second skin arrives moved.**

**Status, 23 Aug 2026 — this is the one piece of the whole document that's actually built.** All Tier 0 (below, §9) shipped as real, working code, not just design: `engine/campaignState.ts` (the campaign-persistent roster, save/load, `evaluatePermadeathCheck`/`applyPermadeathCheck`, `checkMuntiGuarantee`, `canLaunchMission`, `recruitDiscretionary`), wired live into `scenes/TransporterPad.ts` (the deploy gate actually blocks BEAM DOWN when it would launch with no living Munti) and `scenes/Debrief.ts` (a working RECRUIT button that spends points and calls `recruitDiscretionary`). It's the "bare Act I screen" this section describes, functioning exactly as planned, with none of the room skin yet. Everything else in this document — Providence, the Antfarm identity, all five named rooms, the Vault, Energy, `HubScene`/story moments, and every bay in §11's build grid — is still 100% design, zero code. **(23 Aug 2026, later the same day: §12 redesigns WHEN the room skin arrives — rank-gated from Act I, not a flat Mission 14 wall — but doesn't change this paragraph's actual claim. Still zero code either way.)**

## 7. The story-moment layer

This is the actual Sunrider ask, and it's the smallest technical lift in this document, because the campaign already has the exact right-shaped tool for it. GDD §7 defines `MissionEvent` — a flat trigger/action data record, no special-cased code — specifically so scripted moments stay data. A hub scene is the same idea at a different trigger point:

```
interface HubScene {
  id: string;
  room: "hangar" | "workshop" | "vault" | "berths" | "cic";
  trigger: { type: "after_mission" | "heirloom_unlock" | "roster_change";
             missionId?: string; pilotId?: string };
  participants: string[];       // pilot ids
  beats: { speaker: string; text: string }[];   // no branching in v1
  once: boolean;
}
```

**No branching dialogue in v1.** A tree multiplies writing cost fast and buys very little at this campaign's stage — flag it for later (§9), don't build it now. This is the same "prototype the small version first" instinct the campaign doc already applies to itself in its own Appendix C.

**No new art.** GDD §12.2 already solved this problem for the exact reason it's about to come up again: "confirmed no artist for Sunrider-style hand-painted portraits... a coloured circle with two initials. Real portraits are a later drop-in against the same field." Hub scenes use that same placeholder, just with more screen time and a name-plate. The art decision doesn't need to be reopened; it needs to be reused.

**Reconciling this with the "briefings never editorialize" rule.** GDD §10.1 is explicit that mission briefings state the task and nothing else — "a single extra clause of atmosphere would do the narrating the discipline exists to prevent." That rule is untouched and this layer doesn't argue with it; it's a different room doing a different job. The Qiraki side of this project already has the exact craft rule this needs, and Spitball Ideas already declared it the game's tone too, by default: *"warmth lives in the dialogue, the dark lives in the background."* Briefings are the background. The hub is where the dialogue lives. Two rooms, one house, already-agreed rule — this doc isn't asking for a new tone decision, just pointing at the one that's already made and applying it somewhere it hasn't been applied yet.

**Gap flagged, 23 Aug 2026, not resolved here:** `HubScene` above is fire-once and trigger-based on purpose — it's not built to cover "walk up to a squadmate and talk to them more or less any time," the Mass Effect: Normandy-style ask Spitball Ideas' expanded social-layer entry describes. That's a second, smaller data shape (something like a rotating ambient-line pool per pilot, keyed to their current state) rather than a reason to reopen "no branching dialogue" — see that entry for the reasoning, not designed here.

## 8. Beat placement across the 36 missions

Concrete placements, tied to beats the campaign doc already named. This is a starting pass, not a full script — enough to prove the room-per-scene idea against real content before committing to writing all of it. **Consistency note, 23 Aug 2026:** this table's own first row (a Hangar Deck scene after Mission 1) was already quietly assuming the hub existed in Act I, a full section before §1 originally said it didn't — §12 resolves that tension in the table's favor rather than the other way around.

| After mission | Room | What it's for |
| --- | --- | --- |
| 1 — Muster | Hangar Deck | First-time-in-a-lance banter. Mission 1 establishes all five voices in combat; this is the social equivalent |
| 5 — Foraging Party | Berths | Restock-not-death, felt rather than mechanical — someone processing having watched a squadmate go down and come back |
| 8 — The Choir Sings | Workshop | Post-mid-boss banter; a natural slot for a Rourke/Iyari (Foxfire) rivalry beat, given her established competitive streak |
| **12 — The Fallow Line** | **Vault** | **Load-bearing, not skippable.** Requiem's transfer from Bosk to Rourke — or whoever actually holds the gate, per §6a's permadeath note above. The campaign doc wants "the friend-or-foe rule read as grief rather than as game design" — this scene is the only place that reading actually gets delivered |
| 13 — New Colors, Old Wounds | Hangar Deck | The second lance arrives; direct payoff for the roster-growth beat the mission title is already about |
| 17 — The Wellroot Uncovered | CIC | House Amaranth's collaboration becomes visible; political-complexity beat matching Act II's "moral weight of a civil war" |
| 20 — Marrow's Line | CIC | Processing the first named mech-vs-mech duel; a strong slot for Anand (Farsight), already established as "the company's real mentor once Bosk is gone" |
| A-tier unlocks, whenever they occur | Vault | Each Heirloom dedication scene, data-driven off the unlock event, not tied to a specific mission number |
| 24 — Two Fires (Act II finale) | CIC or Hangar Deck | Rourke's promotion to Major; the tone hinge into Act III's grimmer register |
| 29 — The Outer Ring Falls | CIC | Deliberately short and grim — this is where Act III starts earning the tonal shift the doc names outright at Mission 33 ("tone shifts from 'win' to 'survive'"). This scene shouldn't try to comfort anyone |
| 31 — The Last Convoy | Berths | Quiet, earned rest. The Qiraki side of this project already models exactly this need mid-siege — the gigafish sequence's own beat sheet calls for "3-4 chapters of genuine rest and recuperation, earned downtime, not filler, the reader needs the breath as much as the characters do." Same instinct, same place in the structure |
| Before 36 — Until Relief | Vault | Meridian's Vow — the battalion-wide Heirloom "reserved narratively for the finale" gets its dedication scene here, explicitly, before the last mission spends it |

## 9. Build cost, honestly

**Tier 0 — engine/data only, build now, no UI attached (new, 22 Aug 2026).** The permadeath rule (live Munti check on every downing), campaign-persistent roster state (nothing like this exists in the codebase yet — confirmed 22 Aug 2026, no localStorage, no debrief scene, no Fabricator mid-mission wiring either), the deploy gate's validation logic, and the emergency/discretionary recruit functions from §6a. This has no room fiction, no Providence, no art — it's the rules Act I needs to be true starting Mission 1, built as plain TypeScript against the engine the same way everything else in `engine/` is. Genuinely urgent, unlike everything below. **Shipped, 23 Aug 2026 — see §6a's status note. The only tier of this whole document that's actually code.**

**A third Tier-0-adjacent exception, 25 Aug 2026 — Morale/Stress/Favorability's engine-and-data layer.** Same shape as permadeath above and Hangar Deck/Berths in the Tier 1 note below: the mechanical half of Morale, Stress, and Favorability (the stats themselves, Morale's damage modifier, Favorability's pair tracking and mission-together accrual) is engine work, not hub work, and per Maxime it can't sit dark until Act II without undercutting Mission 12's Fallow Line — see §11.4's own update and §13.2's own update for the full reasoning. Ships alongside Tier 0, same plain-TypeScript, no-room-no-art treatment. What still waits for the rank-gated rollout below is only the room-dependent piece — Rec Room minigames, the ship-bar drink, the CO's grotto conversations — not the numbers themselves.

**Tier 1 — cheap, build alongside Act II's own prototype pass.** A room-based reskin of the existing debrief/shop screen (Hangar + Workshop only), the `HubScene` data type, and the Mission 12 Vault scene written by hand because it's the one that has to exist for any of this to be worth doing. No new resource yet. Existing placeholder portraits. **This is where Tier 0's plain Act I screen gets its Antfarm skin and Providence's identity — the underlying calls don't change, only what's drawn around them.** **Superseded, 23 Aug 2026 — see §12.** Tier 1 no longer waits for Act II. The hub is the social/story layer, and per Maxime, that's important enough to move up rather than sit dark for thirteen missions. §12's rank-gated version of Tier 1 — 2nd-Lt scope: Hangar Deck + Berths, the two rooms already functionally live per §6a's Tier 0, now given a room and scenes to go with the mechanics they already run — is recommended to land WITH Act I's own prototype pass, as a second Tier-0-adjacent exception alongside the recruit-phase engine, not held for later. The rest of Tier 1 (Workshop's own room skin, the Vault, the Mission 12 dedication scene) still lands at the Captain-rank seam §12 defines, which happens to fall almost exactly where this section's original Act II recommendation already pointed — so the actual delivery schedule for the *heavier* half barely moves. Only the framing, and the earliest slice, does.

**Tier 2 — moderate.** Energy, the Forward Battery / Auxiliary Berths / Fabrication Bay Expansion / Combat Medic Cadre / Reserve Muster / Vital Signs Uplink modules, the Vault's dedication trigger wired to the existing A-tier unlock condition.

**Tier 3 — later, don't build yet.** Branching dialogue, an affinity/relationship layer, the Mess Deck, animated portraits, and — much later — exposing hub scenes to the player-authored mod kit already parked in Spitball Ideas. **The romance mechanic Spitball Ideas now parks (23 Aug 2026) plausibly belongs here too, alongside "an affinity/relationship layer" — not itemized separately since that entry already covers the shape; flagging so this Tier 3 line doesn't read as having missed it.**

Recommend Tier 1 lands alongside Act II's prototype, the same seam where ship fire-support and the composition choice both already come online per the campaign doc's own Appendix — not before Act I is proven, matching Appendix C's own "prototype Act 1 first" advice. This doc doesn't override that sequencing; it just has somewhere to plug in once that sequencing gets there. **Tier 0 is the one exception to "not before Act I is proven" — it's not narrative hub work, it's the rules engine, and Act I can't be played honestly without it now that permadeath is real.** **(Superseded by the Tier 1 note above, 23 Aug 2026 — Hangar Deck and Berths are now a second exception. Left in place rather than deleted so the doc's own reasoning stays legible: this was the right call when Tier 1 meant the *whole* narrative layer landing at once; §12 splits Tier 1 into a rank-gated rollout instead, and only the first slice of that rollout jumps the original queue.)**

## 10. Open questions — your call

- **Providence-as-Antfarm** — the whole document leans on this. Easy to swap for a new, unnamed ship if that reads better once there's more of the campaign written; nothing else here would need to change structurally.
- **Mandatory vs. skippable scenes.** Mission 12's Vault scene reads as mandatory to me for the reason stated above. Everything else I'd default to skippable-but-logged (so a player who skips can still read it later from the Vault or a "ship's log"), but that's a real tone call, not a technical one.
- **Does Energy ever touch Meridian's Oath?** As written, Energy is Providence-only. Whether Act III's capital ship gets its own pool, shares this one, or stays outside this system entirely is untouched here.
- **Salvaged-vs-lost Heirloom handling**, per §4 — flagged, not resolved. **Sharper now that permadeath is general rather than one scripted loss.**
- **Act I's plain meta-screen, new 22 Aug 2026** — does it get any visual identity at all (even a placeholder "Fallow Line command post" header) or is it deliberately as bare as possible to keep the Antfarm's eventual reveal feeling like a real upgrade rather than a coat of paint on something that already looked finished? **Partly overtaken by §12 — Act I now gets a real, if small, room skin (Hangar Deck + Berths) rather than staying a bare functional screen throughout. Still open: whether that early skin gets its own placeholder header/identity before "the Antfarm" name is earned, or borrows the name immediately.**
- **The build-grid pass (§11, added 23 Aug 2026)** rolls up its own open questions there rather than duplicating them here — the CO's identity chief among them.
- **Rank-gated hub access (§12, added 23 Aug 2026)** rolls up its own open questions there too — the exact bay-slot counts per rank tier, and which of §12.2's two Providence-timeline readings to commit to — not duplicated here.
- **The social-layer texture (Spitball Ideas, added 23 Aug 2026)** rolls up its own open questions in that doc rather than here too — who's romanceable, what romance does mechanically, whether Rec Room drinking carries its own trade-off, and the shape of a repeatable "talk to crew" data type. **Partly resolved, 23 Aug 2026 — Spitball Ideas now has an answer for which two species are excluded from Rourke's romance track (Hiopi, Carabil) and what they get instead; still open there: the exact roster-wide romanceable list, and whether the hub CO (§11.3) ending up Carabil would explain the exclusion.**

## 11. The build-grid pass — twelve bays and the CO's grotto (added 23 Aug 2026)

*Maxime's framing, verbatim: "an XCOM type ant farm... add new room to it as I think of them... thing player can build as soon as they can in act 1, except for arc specific locked thing." This section turns that into the doc's existing shape rather than starting a second, competing system.*

**Status, 23 Aug 2026: this entire section is still design, zero code.** Nothing under §11 — no bay, no Generator, no grotto, no CO — exists anywhere in the engine yet; it's all still a points-spend concept on paper, same as it was when this section was written. Don't read the "Resolved" annotations below as "built" — they mean Maxime made the design call, not that anyone's coded it. The only part of this whole document that's actually shipped is Tier 0's recruit-phase system, covered in §6a's status note and §9. **(§12, written later the same day, redesigns when this section's bay grid becomes buildable at all — rank-gated from Act I rather than waiting on the grotto/Antfarm skin. Still zero code either way.)**

### 11.1 How this fits what's already here

§2's five rooms don't change — they're still the answer to "which room is a given `HubScene` set in," the narrative/scene-trigger layer §7 defines. What's new is a second, physical layer nested inside that fiction: a twelve-bay build grid, the literal XCOM-Avenger-style thing §3's Carrier Upgrade Modules table was always describing in the abstract (buy with points, no spatial representation) but never actually drew. The existing seven modules become bays in this grid rather than a separate system — nothing about their costs or effects changes, only that they now have a place. §2's rooms are still where the *scenes* happen; several of them plausibly contain some number of the twelve bays — the Workshop room holding the Fabricator, Weapons Bay and Generator, CIC holding the Sensor Array and the Beacon control, and so on. That room-to-bay mapping is a recommendation, easy to redraw once the bay list below stabilizes — not load-bearing yet.

**The grotto is new and separate from both systems** — not one of the twelve bays, not one of §2's five rooms. It's the fixed centre the grid is physically built around: the carrier group CO's own post.

**Sequencing note, same shape as §6a's two-skins pattern:** bay *construction* starts Act I like everything else Tier 0 covers — arc-locked bays (a Weapons Bay tied to the Mission-14 unlock, say, the same way Forward Battery already can't be bought before it) excepted. The *grid itself* doesn't get the Antfarm/Providence skin until Act II, same as the rest of the hub. Act I plausibly sees a bare, unbranded build-list; Act II is when it becomes a drawn twelve-bay deck plan aboard Providence. **Cross-ref, 23 Aug 2026 — see §12:** this "bare Act I list, full deck plan later" shape turns out to still be correct even under the rank-gated redesign, it just needed real numbers — §12 gives Act I's list an actual size (small, light-bays-only at 2nd Lt.) rather than leaving "bare" undefined, and the full drawn twelve-bay plan still naturally waits until there's enough rank/space built up to make drawing it worthwhile, which lands around Captain rather than strictly "Act II."

### 11.2 The twelve bays — running list

Provisional, per Maxime's own framing ("add new room to it as I think of them," "etc.") — this is the list so far, not a final twelve, and the existing seven §3 modules aren't repeated here in full (see that table for their costs and effects).

| Bay | Job | Notes / open questions |
| --- | --- | --- |
| **Weapons Bay** (new) | Flat 50% cooldown cut on the Mission-14 ship weapon, base tier | **Resolved, 23 Aug 2026:** "reduce cd on 14 weapon by half" — a flat number, not a menu of options. Confirms the ability itself still unlocks at Mission 14 exactly as §1 already had it; this bay only makes it fire more often. **Overlap with Forward Battery, resolved same day:** the two aren't competing anymore — Forward Battery (§3) is now built as an upgrade *on top of* Weapons Bay, adding another 25% cooldown cut for 75% total. Bay build order matters here: Weapons Bay first, Forward Battery second. **Generator-dependent, confirmed 23 Aug 2026** — see the Generator row below: this bay needs Generator capacity built up before it can actually run, not just be purchased. |
| **Fabricator** | More spare parts | §3's existing Fabrication Bay Expansion, just given a bay rather than staying an abstract purchase. No new mechanic. Generator-dependent (confirmed by name — see the Generator row). |
| **Generator** (new) | Powers the grid | **Resolved, 23 Aug 2026:** "allow you to power fabricator and other heavy room" — the genre-faithful option. Generator is a real power budget: heavier bays need Generator capacity built up before they can actually run, not just be purchased. **Itemized, 23 Aug 2026 — Maxime: "we flagged one building needing a generator, need 2 more to be flagged so... no need for more right now. this isnt completely xcom where you build from day one."** Read as: confirm two more of the bays already sitting on this list as Generator-dependent, not add new bays, and stop there rather than working through the rest of the twelve — this doc isn't trying to fully spec an XCOM-style power-and-adjacency base game on day one. The two chosen are the ones this row had already named as presumed candidates when it was first resolved: **Weapons Bay** and **Long-Range Sensor Array** (see their own rows for the cross-reference) — flagging that call here explicitly in case a different pair was actually meant. That makes three confirmed Generator-dependent bays total (Fabricator, Weapons Bay, Sensor Array); the rest of the twelve are Generator-independent unless revisited. This gives the twelve-bay grid a real build *order* for the first time — Generator likely has to go in early, possibly first, or later bays sit built-but-unpowered. Separate from §5's Energy (the ship's per-mission fire-support/defense resource) — Generator capacity and Energy are two different pools unless told otherwise. **Per §12, Generator is also plausibly the first heavy purchase a 2nd Lt.-tier player can actually afford space for, rather than something Act I never sees at all.** |
| **Long-Range Sensor Array** (new) | Removes fog of war from mission maps campaign-wide, from the moment it's built onward | Maps cleanly onto what the engine already does: `engine/ai.ts`'s `unitsVisibleToSide` currently gates only *hostile unit* visibility — terrain was never fogged to begin with, per that function's own doc comment. This bay would just stop gating unit visibility. Burrowed (Undertow) and concealed (`abil_ambush` / `abil_screen`) states stay hidden regardless, exactly as specified — those are a separate check in the same function and this bay has no reason to touch them. **Generator-dependent, confirmed 23 Aug 2026** — see the Generator row above. |
| **Resupply Beacon** (new) | Lets the player designate a spawn/resupply point anywhere on the mission map | **Confirmed separate from Restock Room, 23 Aug 2026** ("beacon and restock. yes separate"). Still reads as an activated ability, not a passive stat — needs its own rules once it's time to build it: once per mission? Costs a unit's action to place, or free? |
| **Restock Room** (new) | Reduces the after-effects of a restock | **Resolved, 23 Aug 2026:** "restock help reduce the after effect of a restock" — not general resupply. This is the mitigation lever for whatever cost currently attaches to a *restock event* specifically — most likely §6a/§8's emergency Munti replacement (§8 already calls Mission 5's beat "Restock-not-death"), possibly extending to other forced-replacement cases as the recruit system grows. What the "after effect" actually is isn't defined yet — a stat/tier penalty on the replacement? A stress hit (see §11.4)? Flagging the link to §11.4 rather than assuming it, since stress is the obvious candidate once it exists. |
| **Rec Room** (new) | Reduces pilot stress | Confirmed as one bay covering both phrasings from the original ask ("reducing combat stress" / "a game room for less stress"), not two. This is also §2/§9's Tier-3 Mess Deck, un-parked — that section called a no-mechanical-function mess deck "worth wanting and worth not building yet"; it now has a mechanical job, which changes its tier. The stress system itself — what it is, what triggers it, what it costs unmanaged — is its own thing now; see §11.4. **Activity confirmed, 23 Aug 2026** — Spitball Ideas' social-layer entry gives this room its actual fictional texture (drinking), plus an open question worth reading there: flavor-only, or its own small mechanical trade-off. **Sharpened, 25 Aug 2026 — see §13.2's own update:** the three named Rec Room activities (Poker, Fletchers, the peg board) are all confirmed now; still flavor-only as far as this room's job goes. |

### 11.3 The grotto and the CO

The carrier group CO's own post — where carrier-modification approval and, per Maxime, advice both live.

**Mechanically, resolved 23 Aug 2026:** automatic, for now. Maxime: *"co approval. should be automatic. for now."* The CO signs off, no gameplay effect beyond the bay's existing point cost — the lighter default this doc already flagged as the safer call, now confirmed rather than just assumed. **Still true under §12** — rank is the real gate on what's buildable; the CO's sign-off stays a flavor rubber-stamp on top of that, not a second check. **Cross-ref, 23 Aug 2026:** a second CO job is now on record too — Spitball Ideas' social-layer entry parks the CO as a Stress-relief conversation partner once the grotto opens, kept deliberately separate from the relationship-meter half-joke directly below so "can I date my CO" doesn't quietly become the answer to "does my CO help me cope."

**Future hook, on record even though it was floated half-joking:** *"later itl be its own dsting sim (i'm joking but I'm thinking of stealing dragon age origin's love bar)."* Read straight: a real CO relationship/approval meter down the line, Dragon Age: Origins party-approval style — where the CO's standing toward the player could eventually gate or color things instead of rubber-stamping every build. Same treatment as Stress in §11.4: worth writing the intent down now so a future pass isn't reverse-engineering it from a chat log, without designing the actual meter, triggers, or effects here.

**Narratively:** the CO's advice function slots next to CIC/Bridge's existing job (§2: "campaign-plot beats, briefing-adjacent in subject, never in tone") without duplicating it — CIC is politics and the war's shape, the grotto is the player's own commanding officer, a different register, closer to Berths' "the people rather than the war" than to CIC's.

**Name resolved, 23 Aug 2026: Arangement of Content.** Recorded as typed — flag if that should read "Arrangement." Amaranth Reckoning is explicitly the non-canon parallel campaign (this doc's own header, and `MapSelect`'s own subtitle), so naming a CO fresh for it rather than reaching for an existing canon character keeps the line this project has otherwise been careful to hold. **Species and voice still open** — the name itself already reads as a deliberate choice (not a typical person-name, closer to a title or designation), worth building the rest of the character around rather than treating as incidental. Same "your call" status as the other character gaps this doc has left standing. **Cross-ref, 23 Aug 2026:** worth reading alongside Spitball Ideas' fresh romance/bromance note before this gets decided — Qiraki's own carrier-CO archetype (Coherence of Process) is Carabil, one of the two species that entry just carved out of Rourke's romance pool specifically because Carabil aren't humanoid. If this CO ends up Carabil too, the two design calls reinforce each other (a non-romanceable species playing the non-romanceable mentor role); if not, no harm done — the exclusion already stands on its own reasoning either way. Flagged as a connection worth noticing, not a decision made here.

### 11.4 Stress — the shape, not the numbers (added 23 Aug 2026)

Maxime, on why it isn't called Will: *"I dont think we can straight up use 'will' thats why I was calling the mechanic stress."* Keeping that name. This is a real, intended system — not fully designed yet, and per Maxime, that's fine for now ("we just need the basic for now. but we should probably plug this in") — but it needs a shape on record so nothing downstream (Rec Room, Restock Room's "after effect," the recruit-phase system in §6a) is built blind to it.

**Cross-ref, 25 Aug 2026:** a candidate mechanism for computing how a pilot actually reacts moment to moment — the thing that would feed a Stress change here, not just Favorability below — is now written up in `claude/Bloom_Wars_NPC_Reaction_Engine_v1.md`. Design only, not yet wired to anything in this section; see that doc's own §4 for exactly what's still undecided about the connection.

**What's actually decided:**

- **A per-pilot stat that doesn't exist in the codebase yet.** Same category of gap as permadeath before §6a — needs engine/data work, not just a room.
- **Not an early-game problem, by design.** Maxime: *"something thats generally a problem by mission 15 not something thats a problem early."* Reads as an Act II-onward system — notably, right around the same seam where the Antfarm identity itself comes online (Mission 14) and Weapons Bay unlocks. Worth treating that as one seam, not a coincidence: Act I plays clean, Act II is where the ship *and* the pressure both become real.
- **What high stress does — not damage, not death.** Maxime's own reference point is XCOM's Will/panic system: *"something to either rotate crew or force player unit into unfavorable position like will did in xcom."* Two levers, not one: (1) a soft push toward benching an overstressed pilot rather than deploying them — a crew-management pressure, felt at the roster screen; (2) an in-mission effect that puts a stressed unit somewhere the player didn't choose — XCOM's panic (a unit acts against orders) is the direct model, exact form TBD.
- **A future hook, explicitly parked:** *"i want to introduce ctulu mental effect by certain unit they are just not coded in yet."* Certain units (unspecified — presumably Qiraki-side entities with a cosmic-horror register, matching this project's own established tone elsewhere) are meant to inflict a heavier, Lovecraftian flavor of mental effect on top of baseline Stress. Not designed here — those units don't exist in the engine yet, so there's nothing to attach it to. Flagging the intent so Stress's basic version gets built with room for a nastier tier later, rather than needing a rework to add one.

**Update, 25 Aug 2026 — engine-tracked from Mission 1; narrative weight still ramps toward Act II.** Maxime: "I think we are failing the purpose of all this if we dont make moral and stress and love something taken into account from the start of the game." Correct catch, and it doesn't actually conflict with "not an early-game problem" above — it corrects how that line was being read. "Not an early-game problem" was always a statement about Act I's *tone* (it plays clean because nothing catastrophic has happened yet), not a build-order instruction to leave the stat itself switched off until Mission 15. Read the other way, it silently disarms the one scene this document calls load-bearing: Mission 12, the Fallow Line, is explicitly meant to "read as grief rather than as game design" (§8) — that only works if Stress (and Morale, and Favorability, §13.2) have real numbers behind them by the time that mission fires, which means they have to be live well before it, not starting at it. Same split this document already used once for exactly this shape of problem — §6a's "two skins, one system" for permadeath: the *engine and data* (the stat itself, its combat effects — Morale's damage mod, Stress's own eventual bench/panic levers once designed) go live from Mission 1, quietly, no room, no UI beyond what combat already needs. What still waits for Act II is the *narrative* layer this document actually gates — Rec Room as a place, the CO's grotto, the hub scenes. A third Tier-0-adjacent exception alongside permadeath and Hangar Deck/Berths — see §9's own note. Practical consequence, stated plainly rather than left implicit: Stress readings will likely start registering before mission 15 in practice now, simply because the numbers are real and Mission 12 is a genuine loss. That's not a bug in this update, it's the point — mission 15 was always Maxime's estimate of when it becomes a *problem* narratively, not a wall the number was supposed to sit behind.

**Still genuinely open, not guessed at:** what raises Stress (a downing witnessed, a Collapse, a failed extraction, a restock — see §11.2's Restock Room), whether it decays on its own or needs the Rec Room actively, whether it's tracked per-pilot or company-wide, and the actual numbers for either lever. Same restraint this doc already shows with Energy and Severance — the shape's on record, the math isn't, because guessing at math nobody asked for is how a "basic version for now" quietly turns into a full system nobody agreed to yet.

**Cross-ref, 23 Aug 2026:** Spitball Ideas' expanded social-layer entry adds a second stress-relief lever on record alongside Rec Room here — conversations with the CO, once the grotto opens (§11.3) — plus a much sharper trigger worth folding into "what raises Stress" above: losing a romanced pilot permanently, which that same entry flags as needing a bigger spike than an ordinary loss. Not designed here either; just making sure this section's own "still open" list doesn't go stale the way the top of this doc's "fear and motivation" note briefly did.

## 12. Rank-gated hub access — buildable from Act I (added 23 Aug 2026)

*Maxime: "lets make the ant farm something you can build as soon as act 1. but your gated on space and type of room until you get promoted... its the social hub so its kinds impirtant." Supersedes §1's original "Act I has no hub" call and §9's "Tier 1 waits for Act II" recommendation — see those sections' own updated notes. This section is the actual gating design, and like §11, it's still 100% paper — nothing here is built.*

### 12.1 The gate: rank, not mission number

Reuses a track this project already locked rather than inventing a new currency. The campaign doc's own §10 squad-scaling table already pairs roster size, off-board support, *and* rank in one row per act — and Rourke's own two promotions are already scripted, named beats: Captain at Mission 12 (the Fallow Line, Act I's finale — the same mission that's already the Vault's one mandatory scene, §8), Major at Mission 24 (Two Fires, Act II's finale). Hub access rides that same track instead of a flat Mission 14 wall:

| Rank | Missions | Rooms live | Space (buildable bay slots) | What's still locked |
| --- | --- | --- | --- | --- |
| **2nd Lt.** | 1–11 | Hangar Deck, Berths — both already functionally live per §6a's Tier 0, this just gives them a room and a scene. Workshop's *existing* half (gear tier, spare parts, mek secondaries) is also already live pre-hub (it's `Debrief.ts` today) | Small — first pass, 2-3 bay slots, light bays only. Generator (§11.2) is plausibly the first heavy purchase once space allows, rather than something Act I never reaches at all | Vault (no A-tier Heirloom is reachable this early; Requiem specifically stays locked until Mission 12 per already-locked canon), CIC (no ship fire-support exists yet to configure) |
| **Capt.** | 12–23 | + Vault, unlocking with the promotion itself rather than sitting open before it — Mission 12 *is* the Requiem dedication scene, so the rank-up and the Vault's first real use are the same beat, not two. + CIC, specifically from Mission 14 onward (ship fire-support's own already-locked unlock, two missions into this rank rather than at its start) | Medium — roster's doubled to 10 across two lances by now (§10); space should track that | The full twelve-bay grid still caps below Major; the Generator-dependent bays (§11.2) plausibly stay the ceiling of this tier rather than a Major-only reserve, but that's a first-pass guess, not itemized |
| **Maj.** | 24+ | All five rooms | Full — the whole twelve-bay grid, matching Act III's ~20-pilot/4-lance scale (§10) | Nothing hub-side; Meridian's Oath (the separate Act III capital ship, §10's own open question) stays its own untouched system |

**Flagged, not locked:** the exact slot counts ("2-3," "full twelve") are first-pass placeholders in the same spirit as this doc's other unset numbers (Energy's cap, Stress's math) — the *shape* (grows with rank, matches the roster/support numbers already in §10) is the actual design call; the specific integers are cheap to retune once there's an actual build list to point them at.

### 12.2 Providence's own timeline, untangled

The thing that made "Act I has no hub" true in the first place was never really about rooms — it was "Providence isn't forward-deployed yet." Resolving that means actually separating two claims this doc had been treating as one: **Providence existing** and **Providence being able to shoot at things**.

Recommendation, same "easy to veto" status §1 already used for the Providence-is-Antfarm call itself: Providence is present from Act I as Warden Company's rear logistics support — a WW1-style trench line holding a static front plausibly has *some* rear echelon from day one, that's not a stretch — and only its fire-support call-in ability stays gated to Mission 14, exactly as already locked and unchanged by any of this. The squad-scaling table's "off-board support: none" for Act I still reads true under this — it's about the *ability*, not the *ship*.

The alternative, on record rather than silently discarded: Act I's hub is a distinct, unnamed forward command post that later gets folded into or renamed Providence at Mission 14. Also invents nothing extra, and might read better once more of Act I is actually written — flagging it as the fallback rather than picking one and hiding the fork.

### 12.3 What this doesn't change

- **Tier 0's engine work** (§6a, §9) — already live from Mission 1, already shipped in code, untouched by any of this. This section is entirely about when the *room skin and scenes* land, not the rules underneath them, which were never gated on Providence to begin with.
- **The CO's grotto** (§11.3) — automatic approval stays automatic; rank is the real gate here, the CO's sign-off is still flavor, not a second check.
- **Arc-locked bays** (Weapons Bay tied to Mission 14, per §11.1) — still locked to their own mission triggers regardless of rank; a Captain two missions early doesn't jump the Mission-14 queue.
- **Mandatory vs. skippable scenes** (§10's open question) — untouched; Mission 12's Vault scene is still the one load-bearing, non-skippable beat, now simply arriving exactly when Capt-tier access does instead of arriving as a stray unlock inside an already-open Vault.

## 13. The hub as a walkable space, and Favorability (added 23 Aug 2026)

*Maxime: "i want the hub to me like a city you can move your unit in. allowing you to visit other characters. mek and synkers and the co i wana steal arguably the best social system. but we arent doing complicated thing like in baldur fate. we are fighting the wnd of the world. so a simple bar like in DAO. dragon age origyn is fine." Two asks in one message — a presentation-layer change (how you move through the hub) and a mechanical system (Favorability) — kept as two subsections since they're separable design calls even though they were pitched together.*

### 13.1 A walkable hub, not a menu — scope flag

**Worth flagging before this goes any further, per the project's own standing rule: this is a meaningfully new system, not a reskin.** Everything §2 through §12 designs — the five rooms, the twelve-bay grid, the grotto — is a *location model* (discrete places with a mechanical job and a narrative job) presented through menus and `HubScene` pop-ups. "Move your unit in a city" is a *different interaction mode*: a small explorable space with an avatar, movement, collision, and NPCs to walk up to — closer kin to the tactical battle grid (`engine/grid.ts`, the Battle scene's tile rendering) than to a debrief screen, just for a non-combat space instead of a mission map. That's real, new engineering surface — a second movement/rendering mode alongside the one that already exists for missions — not a coat of paint on the room menu.

**The good news: it doesn't invalidate anything already designed.** The room/bay/grotto model everything above this section builds on is location-based already — a walkable hub city is a compatible *presentation* of that same model, not a competing one. Hangar Deck, the Workshop, the Vault, Berths, CIC, the twelve bays, and the grotto all plausibly become physical places on one small walkable map instead of menu entries — same jobs, same gating (§12's rank/space rules untouched), just reached by walking instead of clicking. Nothing in §1–§12 needs to be redesigned; this section is additive, describing *how you move between things that already have a design*, not replacing what those things are.

**Not scoped or started.** Same Tier 3 status as branching dialogue and the rest of §9's "later" bucket — flagged here so it's on record with real shape, not proposing to build it.

**Open naming note, not urgent:** Maxime's phrasing groups "mek and synkers and the co" as who you'd visit. "Synker" is Qiraki's own established term for a pilot (`Qiraki_Bible_Skeleton.md` §10's terminology list) — not the reserved spoiler term itself and not "The Synker Wars" either, so no naming-lock issue, but it's not a word that's shown up in any Bloom Wars doc before now, and the game's own locked vocabulary already has a word for this role: pilot (`PilotRecord`, Data Pack §2, used everywhere). Worth a quick call whenever it matters: is "Synkers" here just how Maxime thinks of pilots casually, or an actual proposal to rename the game's own term? Defaulting to keep "pilot" in Bloom Wars docs until told otherwise, same one-way-inspiration treatment already given to Hiopi/Carabil/Osnian as species names without adopting their source vocabulary wholesale.

### 13.2 Favorability — a Dragon Age: Origins-style rep bar, not a Baldur's Gate 3 web

**Explicitly scoped down, in Maxime's own words: "we arent doing complicated thing like in baldur fate... a simple bar like in DAO."** One number per NPC relationship (crew, mek techs, the CO — not branching approval-per-choice the way a full CRPG does it), moving up or down, no dialogue-driven approval swings to track. This matches the project's existing discipline of prototyping the small version first (GDD Appendix C, §7's "no branching dialogue in v1") rather than a scope pivot.

**Cross-ref, 25 Aug 2026:** the DAO/BG3 framing above and this bar's own farming mechanics are one design thread; the actual mechanism for computing a pilot's moment-to-moment reaction — which this bar's ups and downs would plausibly be one output of, not a separate calculation — is now written up separately in `claude/Bloom_Wars_NPC_Reaction_Engine_v1.md`. That doc explicitly flags Favorability as a likely *output* of the reaction engine rather than its own independent system; not wired together yet, see that doc's §4.

**Update, 25 Aug 2026 — farming starts Mission 1, not Act II.** Same call as §11.4's own update, same day: Favorability's engine-and-data half (the pair tracking, the mission-together accrual specifically) is live from Mission 1 — Mission 1 ("Muster") is already the beat table's own "first-time-in-a-lance banter, establishes all five voices" moment (§8), so there's no reason bonds wait to start forming. What still waits on the room-gating in §12 is the *venue*-dependent farming sources specifically — Rec Room minigames and the ship-bar drink both need a physical Rec Room, which isn't guaranteed at 2nd Lt. tier under §12.1's table. Fielding pilots together on missions has no such dependency and starts accruing immediately.

**What it's for, mechanically — not narrative-only.** Maxime: "do activities with the npc to gain more favorable rep. and it give them dodge chsnce bonus in combat, keep them morally happy and un stressed and it give them better hit chance in combat." Three real effects on a favored pilot, all in the existing systems rather than a new stat nobody else touches:

- **A dodge chance bonus in combat** — stacks with (doesn't replace) Meeps' existing 40% dodge (`Already built`, above); presumably a smaller bonus available to any class, not Meeps-exclusive, though that's not stated either way.
- **A better hit chance in combat** — **status changed, 23 Aug 2026, see §13.4: the universal accuracy-roll system this would need is now explicitly parked, not just pending.** This bonus stays on record as intent but has nothing to attach to unless a scoped-down version gets built specifically for it — full reasoning in §13.4.
- **Morale/Stress relief** — "keep them morally happy and un stressed" reads as morale, transcribed as typed since it could also mean something narrower; either way this is Stress (§11.4) gaining a third lever, alongside Rec Room and CO conversation (both already cross-referenced from Spitball Ideas).

**How it's farmed — four sources, one of them formerly pending detail.** Maxime: "favorability is farmed by doing mission with them playing with the character in the rec room at one of the 3 mi i games. pokers, fletchers and the peg game zi invented for this world. ill share the peg game later. sharing a dri k at the ship bar."

- **Fielding them on missions together.** The most XCOM/DAO-native source — party comp as relationship investment, not a separate minigame.
- **Rec Room minigames — three of them, all confirmed now.** Poker (needs no invention, real game). Fletchers — reads as an archery/darts-adjacent game given the name, still not detailed beyond the name. The peg game — see the new update immediately below; no longer pending.
- **Sharing a drink at the ship bar.** Ties directly to Rec Room (§11.2) and gives it the second activity beyond drinking-as-flavor that Spitball Ideas left as an open question.

**Update, 25 Aug 2026 — the peg game is no longer pending.** Maxime shared the actual rules: a real, complete two-player abstract strategy game, not a placeholder. A 3×3 dot grid, a locked opening move, and a turn structure where each player draws one straight line per turn between two still-open dots, always continuing from wherever their own last line ended — one continuous, non-self-crossing path per player, and the two players' lines can never cross each other either. Forming an open two-line bend through three dots ("Reach") threatens a win; the opponent gets exactly one turn to close it into a triangle ("Shield") or loses outright. A faster "Momentum" variant makes Shield a free action instead of spending the whole turn, so the defender can shield and keep moving in the same breath. Deeper play has a "Knot" (three lines meeting at one dot, which then locks for the rest of the game) and named shapes (Block, Trap, Wall) the fiction can hang flavor on later. If the board fully jams with no Reach ever landing, most Knots wins; a tie is a draw. All of that is clean, generic strategy-game mechanics — nothing about the ruleset itself is book-specific, and it's confirmed safe to build against.

**Flagged, not resolved: the source material's own flavor text isn't going in this doc.** Maxime's rules document names the opening move's sound ("AH") as part of "the game's secret alphabet," and includes a deliberately unexplained note that tracing a bend backward "feels wrong" for no stated reason. That reads like it could be reaching for Qiraki's own rune-tech material rather than being purely original to this game — exactly the kind of thing the project's naming lock exists to catch before it lands somewhere it shouldn't. Not pulling that layer into Bloom Wars material by default; the mechanics above are what's confirmed safe. Worth a direct call from Maxime either way, not guessed at here. Practical consequence already applied: this doc, and the sandbox below, call it "the peg board," never the source document's own title.

**Wired into the design sandbox, flavor-only — not a playable implementation.** Source lives in the project as `claude/pilot_creator.html`, cross-referenced from `claude/Bloom_Wars_Character_Editor_v1.md`; the live, interactive version is published at `https://claude.ai/code/artifact/980bf45b-dc52-4fa4-bb30-d173a2a8b960`. That sandbox now names "the peg board" as one of three Rec Room activities in its whole-roster event log, alongside Poker and Fletchers, with a few flavor-line variants using Reach/Shield/Knot as plain vocabulary. Mechanically identical to the other two activities — same Favorability/Stress/Morale numbers fire regardless of which one gets picked; nothing plays an actual hand of poker or a real game of peg. Confirmed explicitly by Maxime: doesn't need to be playable right now, only needs to "work with the social experiment." Same status as the rest of this section otherwise: sandbox-only, zero production code, and the actual playable board (non-crossing-line validation, Reach/Shield detection, Knot tracking) stays unbuilt and unscoped until asked for.

**Drinking has a real, named cost, now confirmed with real numbers — resolves an open question Spitball Ideas left hanging.** First pass, transcribed as "(dont get drunk, you'll lose 29%, hit chsnce.)" — the "29%" was a typo, corrected in the same follow-up conversation: "debuff on drunk. something like 20% for a few turn. of less able to hit things." Locked reading: **−20% hit chance for a few turns** (exact turn count still open — "a few" isn't a number yet). This settles Spitball Ideas' "flavor-only, or its own small mechanical trade-off?" question for Rec Room drinking: it's a real trade-off, not pure flavor. **Doesn't need the full universal accuracy system to exist — see §13.4.** Since that system is now parked rather than pending, the drunk debuff is the stronger case for a narrow, scoped implementation: a temporary per-unit accuracy malus that only ever applies to a unit under this one status effect, rather than a general hit/miss roll every attack in the game has to run through.

**Update, 25 Aug 2026 — the sandbox's own loss trigger didn't match the permadeath rule; fixed.** Caught in the same conversation as the sandbox build itself: `pilot_creator.html`'s `triggerLoss()`/`ensTriggerLoss()` were treating every rolled "loss" as unconditionally permanent — Favorability channels deleted, pilot pulled from the roster — with no check against whether a Munti was actually present. Confirmed against the live `engine/campaignState.ts` source that the real permadeath rule stays exactly as documented and shipped: a deterministic, live-checked-at-the-moment-of-downing boolean (any other living Munti anywhere on the field = standard restock; none = permanent), no dice layered on top, per Maxime's own call when the alternative — a probability roll instead — was raised and declined. The sandbox now runs the same check — a living Munti anywhere in the roster besides the pilot who just went down — before deciding which branch fires.

**Update, 25 Aug 2026 — a close call is a joke opportunity, not a trauma beat.** Maxime's own framing for the restock branch above: when the Munti saves someone, that's not staged as a smaller version of grief, it's staged as banter. Own numbers, not the true-loss formula — Stress ticks up 5-10 (the scare was real even if the ending wasn't), Morale actually climbs +6-10 (relief, plus a story to tell), and the pair gets a Favorability bump for surviving it together, same as any other shared experience this section already tracks. The sandbox now picks from a small bank of joke/banter lines for the moment itself, and forces the survivor's dialogue echo to "love" rather than risking the game's own baseline pool landing on fear or sadness for what's supposed to read as levity.

**Update, 25 Aug 2026 — Muntis gain Favorability faster with everyone, not just their assigned partner.** Maxime: "flag Muntis as having a bonus to Favorability gain with everyone." Read as a property of whichever pair a Munti is in, not an ambient buff to pairs they're not part of — same logic as the update above: they're the ones hauling people back from the brink, so it tracks that bonding happens faster around them. Applies to Favorability *gains* only; a bad night at the bar isn't cushioned just because a Munti happened to be there. Wired into the sandbox at its single choke point for Favorability changes, so it applies uniformly across every source in the farmed-from list above — missions together, Rec Room, drinks, the close call bump — not just one of them. 1.5× is illustrative only, same footing as the drunk debuff's turn count and the peg game's still-open exact numbers above: a placeholder, not a locked balance figure. **Worth flagging for the Data Pack too** — if Munti's other class traits (the disintegrator weapon, the passive regen aura) live in that doc's own class writeup, this Favorability bonus is a new one to add alongside them; that file hasn't been opened this session, so noting it here rather than touching it unverified.

**Update, 25 Aug 2026 — the close-call fix above also named the specific Munti, not "the Munti"; that surfaced two more gaps, both closed same day.** First: the joke-line bank only ever said "the Munti," generic class noun, when it already knew exactly which pilot did the saving — rewritten so the credited line names them directly. Second, and the bigger one — Maxime: "the guy must remember who did what when in mission." The Munti who performs a save wasn't tracking it anywhere; a bare counter would have lost the "who" and "when" both. Each Munti now carries their own ordered save log on the pilot record itself — who was saved, and a sequence number standing in for "which mission" (this sandbox has no real mission/campaign counter to check against, so it's an honest placeholder, not a claim about actual mission numbers). Visible on the roster card, not just tracked internally: a "N SAVES" tag, full log on hover. Same day, same thread as the two updates above — a Munti who saves someone now gets their own small Morale bump for it too (separate from the survivor's own relief bump, stacks if the Munti saved themselves), which is what the save log is actually counting.

**Update, 25 Aug 2026 — the save log generalizes to three more tally types, all four classes.** Maxime: "add the same thing to each character, tanks reeps meeps on kill assist and munties lost. add munties lost to munties too. add the saved a fellow soldier (rescue) tally too. and a boost to morale when that happen. look at amaranth mission log and integrate what happen in it too." Same ordered-log shape as the save log above (who, and a sequence number), extended to three more tallies rather than staying a Munti-only pattern:

- **Kill assist — tank/reeps/meeps only, not Munti.** Munti's own tally is the save log; kill assist is the other three combat classes' equivalent. Fires deterministically on every clean mission (no probability gate), crediting the other pilot in the pair by name. Grounded in real engine precedent rather than invented from scratch: `engine/mission.ts`'s own per-pilot `unitPerformance` already tracks `assistCredit` this same way — a running count on the pilot record, not a derived stat — confirmed against the live source 25 Aug 2026.
- **Munties lost — all four classes, including Munti itself.** Fires on a true loss (no living Munti anywhere to trigger the restock branch) for the pilot who went down, if they were a Munti. Deliberately **not** Favorability-gated the way the existing "mourners" ripple in `triggerLoss()`/`ensTriggerLoss()` is (that one only hits pilots with a recorded bond to the victim) — every other living pilot on the roster gets the entry regardless of whether they ever fielded with that Munti. Read as squad-wide knowledge rather than a closeness-scaled reaction: losing your Munti isn't a rumor that only reaches people who liked them.
- **Rescue — class-agnostic, all four classes, new event type.** Not a variant of the close-call/restock branch (that one's about a roster pilot almost dying); this is a positive, proactive find — a downed *stranger* pilot on the field, either one of the paired pilots can make the pickup. Grounded the same way as kill assist: the real `rescue_pilot` bonus objective (Amaranth Missions 5, 9, 16, confirmed against the Amaranth Act I build log 25 Aug 2026) is explicitly class-agnostic — "any adjacent player unit can pick the NPC up, not a class verb like Repair" — so the sandbox's rescue tally follows suit rather than restricting it the way kill assist is restricted. New button ("Field rescue attempt ★") in the direct-pair sandbox, plus a headless version wired into Simulate a Day (rebalanced odds: mission 50%, Rec Room 20%, drinks 20%, rescue 10%, illustrative only, same footing as the other percentages in this section). Smaller Favorability bump than the close call's own (+5 vs. +8) and a Morale boost (+6-10) for whichever pilot made the pickup — same shape as the Munti hero-credit bump above, generalized to whoever earned it this time.

All three follow the save log's own visible-not-just-tracked rule: roster card badges ("N ASSISTS," "N MUNTIES LOST," "N RESCUES"), full log on hover, same pattern as the existing "N SAVES" tag. **Not built from the mission log for its own sake — used to keep the sandbox honest about what the real game already does,** so these three tallies read as extensions of confirmed engine behavior (`assistCredit`, class-agnostic rescue) rather than sandbox-only invention layered on top of it.

**Update, 25 Aug 2026 — three logical-next-step ideas, approved and built same day ("all 3 of your points are good and should be added").** Raised alongside the tally system above as flagged-not-built follow-ons; Maxime approved all three without changes, so all three are now live in the sandbox:

- **Legend status.** A derived title, not a stored field — `heroicTallyCount()` sums the three *heroic* tallies (saves + kill assists + rescues; muntiesLost stays out of this sum on purpose, it's a loss, not an achievement), and a pilot crosses into Legend at 5+ combined (illustrative threshold, same footing as everything else in this section). Visible two ways: a "★ LEGEND" roster-card tag in the sandbox's own accent color (kept distinct from the green "hero" tags the tally badges already use, so Legend reads as a status, not another tally), and a matching ring around the pilot's portrait.
- **Munties-lost actually does something.** Previously a passive log (§13.2's own update above). Now feeds two live effects: a cumulative Stress toll on `recordMuntiLost()` itself — each loss adds its own tick, scaling with how many times *that specific pilot* has already been through it (2, 4, 6, 8... — the 3rd hits harder than the 1st) — and, at 3+ losses, a bias in that pilot's dialogue-echo pool toward fear/sadness (`pickEchoForPilot`/`pickSoloEchoForPilot`), sitting below the existing acute-state checks (drunk, panicking, low morale still take priority) but above the baseline/close-bond roll. The Stress toll deliberately stacks with whatever else that same loss event already did to that pilot (the survivor's own acute hit, a Favorability-gated mourner tick) — read as "I was there" and "this keeps happening to me" being two different things, not a double-count bug.
- **Cross-tally callouts.** An occasional aside referencing a pilot's own history instead of every line reading as a fresh, disconnected event — "the second time Patch has hauled someone back from the brink," that kind of thing. Fires from the 2nd occurrence of a given tally on (never the 1st — nothing to call back to yet) and only about half the time even then, so it reads as something that sometimes comes up rather than a guaranteed tagline. Wired into the two sandbox flavor-line generators that already existed (the close-call joke bank, the new rescue line bank) and into the true-loss status message when the victim is a Munti, where it reads squad-wide ("the second time the squad has lost a Munti") rather than per-pilot, since every living pilot's muntiesLost count moves together on that event. **Not wired into kill-assist** — missions don't currently render any custom flavor text (just the two dialogue-echo lines), so a kill-assist callback would mean adding a text slot that doesn't exist yet rather than reusing one that does; flagged rather than built, consistent with keeping this pass to what already had a home to extend.

**Emergent property worth flagging, surfaced by a 120-day Playwright soak test, not something this update changed:** with two or more living Muntis on a roster, true permadeath essentially can't trigger — the restock check only needs *one* other living Munti anywhere, and restocking never actually removes either Munti, so the condition that would allow a real loss never arises as long as both survive. Across 120 simulated days with two Muntis on a 6-pilot roster, nobody was truly lost even once; the roster just piled up heroic tallies instead. This is a direct, correct consequence of the permadeath rule exactly as specified (deterministic, "any other living Munti anywhere" — no probability, no per-Munti cap), not a bug in this update — flagging it here since it's a real balance implication worth knowing about whenever squad composition or Munti scarcity gets tuned for real.

**Update, 25 Aug 2026 — tightened the Rec Room flavor bank and the flavor-to-dialogue gap it sat next to.** Maxime flagged a specific example: the peg board's Knot line ("traps {a} in a Knot on the peg board — {a}'s still muttering about it after") read loose, and the dialogue exchange rendered right underneath it in the ensemble log was a fully disconnected random pick — in his own example, the pilot who'd just been "trapped" got a "DOG · ANGER (drunk)" line about loyalty, and the pilot who'd just won got "CAT · ANGER (baseline)" about not fixing someone's mess. Neither echo had anything to do with the game that was just narrated one line above it. Two separate fixes:

- **Wording.** The Knot line tightens "traps {a} in a Knot... after" to "knots {a}... about it" — the noun phrase collapsed into a verb, the throwaway "after" cut. The rest of the Rec Room bank (Poker, Fletchers, the other peg-board lines) got the same pass — mostly cutting activity names the ensemble log's own header already prints (`REC ROOM · THE PEG BOARD`, etc.), so a line saying "at the peg board" a second time was often just restating the header.
- **The flavor/dialogue gap — explicitly scoped down, not the real fix.** Maxime: "there's gonna be a sentence generator soon... the generator is in construction," meaning the actual content-aware version (dialogue that references what just happened, in its own words) is a separate, bigger system for later, not attempted here. What's live now is mechanical, not generative: a flavor line that already implies a clear winner/loser (currently just the Knot line — the rest of the bank stays a stalemate/no-outcome by design, so most Rec Room dialogue still rolls blind, same as before) carries an `outcome: {winner, loser}` tag through `pickRecFlavor()` → `ensRecEvent()` → `logMissionRecDrink()`, which biases `pickEchoForPilot()`'s tone: the winner leans love/anger (a fond or smug tease, 70/30), the loser leans anger/sadness (grumbling, mostly — sore-loser, not devastated, 70/30). Sits below every acute state in the existing priority cascade (drunk/panicking/low morale/grief-worn still take priority — a drunk pilot's dialogue stays drunk regardless of who just won a card game) but above the passive close-bond/baseline roll. **What this does and doesn't fix:** the visible "(just lost)"/"(just won)" reason tag and the echo's tone now cohere with the scene above it — genuinely tighter, testably so. The actual quoted dialogue *text* still comes from the generic per-animal `LINE_BANK` and doesn't reference the peg board specifically; closing that gap for real is exactly what the future sentence generator is for.

**Update, 25 Aug 2026 — a fifth, unified log: the full social-history record.** Maxime: "add a history of socialisation to a character, its record like in Primal Hunter" — then, once asked to pick a scope (a light merge of the existing tallies, that plus a few relationship milestones, or a full diary), landed on the fullest option without hesitation: "3 its the record and the record take in everything." The four typed logs from the update above (savesLog/killAssists/muntiesLost/rescues) are untouched — roster badges still read straight off them — but every pilot now also carries a `history` array covering everything those four don't: missions fielded together, Rec Room visits (with which activity, when ensemble-simulated), drinks shared, being the one saved rather than the one saving, witnessing a pair-mate's close call, and witnessing a death (both the survivor and any Favorability-bonded mourners). One event that touches two pilots shares one sequence number across both of their entries — a mission both of them fielded reads as the same numbered moment in each record, not two numbers a beat apart. A new "Record" button sits on every roster card (mirrors the existing "Ambient" button) and prints the full diary into the ensemble log as plain, readable lines — this is the actual record Maxime asked for, not a summary of it.

**Update, same day — the shorthand aside.** Separately, mid-build: "find a way to shorthand the memory using the formula as an example" — pointing at the NPC Reaction Engine's own `(A + B) + (a + b4(c)) = D + E` notation as the model. Then, once the full diary above was confirmed, a scoping call on how the two relate: "the shorthand as an aside for something else" — not a replacement for the readable diary, a compact companion to it. Landed as a derived one-line formula per pilot, printed underneath the full diary whenever the Record button fires: entry types collapse to 3-letter codes (MSN/REC/DRK/AST/SAV/RES/CLS/LOS/KIA), directional entries split by an arrow the way the Reaction Engine's own notation leans on symbolic operators over prose (`SAV→` — this pilot saved someone; `SAV←` — this pilot was saved), counts join like terms in an equation, losses (muntiLost, kia) sort last and carry a minus rather than a plus. A pilot who's been through a full campaign reads as something like `13·MSN + 13·AST + 5·DRK + 1·CLS + 6·REC + 1·RES→ − 1·LOS − 3·KIA` — a whole social history in one line, legible at a glance the way the Reaction Engine's own formula is, without pretending to replace the sentences above it.

**Update, 25 Aug 2026 — a Munti-specific baseline loss chance, layered on top of the existing mission roll.** Maxime: "add a 10% chance of muntie loss per mission baseline. to the simulator." Deliberately kept as a second, independent roll rather than folded into `LOSS_CHANCE` (still 20%, unchanged): that number is "does this mission produce a downed pilot at all," picking the victim 50/50 between whoever's fielded. The new `MUNTI_MISSION_LOSS_CHANCE` (10%, same illustrative-only footing as every other number in this section) only rolls when it didn't already fire, and only when a Munti is actually one of the two pilots on that specific mission — same scoping as every other mission-triggered number here (Favorability nudges, Morale/Stress deltas), nothing squad-wide or off-screen. When it hits, that Munti is the forced victim — `triggerLoss()`/`ensTriggerLoss()` both took an optional forced-victim parameter for this rather than getting duplicated — but everything downstream is identical to any other loss: the same live permadeath check (another living Munti anywhere on the roster still saves them into a restock, same close-call joke framing as the update above; none, and it's a true loss with the same grief/Stress/mourner consequences already documented in this section). Net effect on a Munti actually fielded: roughly an extra ~8 points of at-risk probability per mission on top of what the general roll already gave them (10% direct + the ~10% the general 20% roll already had a 50/50 chance of landing on them), gated the same permadeath-check way either roll already was. Verified with a targeted Playwright suite (constants, forced-victim routing on both the single-pair and ensemble code paths, restock-vs-true-loss branching, and confirming the branch is skipped entirely — not just failing its roll — when no Munti is in the fielded pair) before shipping; all passed. **Applies to the week sim too, automatically** — "Simulate a Week (×7)" is literally seven calls to the same `simulateDay()`/`ensMissionEvent()` chain "Simulate a Day" already uses, no separate week-scoped code path, confirmed both by reading the call chain and by a 60-trial soak (a lone, unbackstopped Munti lost within a simulated week 36/60 times, 60%). Also caught mid-thread: the first publish of this change to the live Artifact page failed silently from Claude's side (a sandbox network restriction blocking the artifact host) — Maxime tested against the stale pre-change build and reasonably read that as the feature not working. Force-published the correct version once confirmed; worth remembering this as a failure mode (a reported "doesn't feel like it's working" can mean the publish never landed, not that the mechanic is wrong) rather than assuming the numbers need retuning first.

**Update, 25 Aug 2026 — a manual "stage loss" button, for testing without waiting on RNG.** Maxime: "allow me to stage loss by deleting a npc." New per-roster-card button (top-left corner, labeled "Loss," deliberately separate from the existing ✕ Remove — Remove is a no-consequence undo, this runs the real thing) that puts a chosen pilot through the exact same loss pipeline a random mission roll would: the live permadeath check (another living Munti elsewhere saves them into a restock; none, and it's a true loss), the mourner ripple, the muntiesLost squad-wide tally, and the grief catalyst below. If the pilot is currently sitting in the Social Layer Sandbox's own pair slots, this routes straight through the existing `triggerLoss(forcedVictim)` (same acute survivor beat as any other loss); otherwise it runs a standalone twin with the same consequences minus that one acute hit, since there's no specific fielded partner to assign it to. Verified with a targeted Playwright suite (both branches, both routing paths, deferred-removal timing).

**Update, 25 Aug 2026 — grief catalyst: a loss now decides whether the people left behind grow closer or drift apart, read off their own personality.** Maxime, across two follow-up messages: "loss should make them go through a round of catalyst for them to decide their new relationship... the loss/gain thing should be based on personality." **Scope note, flagged before building rather than after:** `Bloom_Wars_NPC_Reaction_Engine_v1.md` is explicit that the full formula is design-only, nothing built before the hard tactical loop clears (§5 of that doc). This doesn't reopen that gate — it reuses the exact same narrow, already-shipped piece the dialogue test panel has been running on all session (`pickSoloEchoForPilot` — one pilot's animal-list archetype read through the four echoes, no scene context, no A/B, no full formula) for a second purpose, not a bigger one. Mechanically: whenever a true loss happens, every pilot who mourned it together (the survivor plus any Favorability-bonded mourners) gets their own solo echo read on how they're personally taking it, and every *pair* among that mourning group that already has an existing bond gets that bond nudged based on how the two echoes combine — love and sadness lean toward pulling closer together, anger and fear lean toward friction or withdrawal (`ECHO_BOND_LEAN`: love +2, sadness +1, fear −1, anger −2, summed per pair and scaled ×2, so roughly −8 to +8, illustrative only). Lazy-init respected throughout — a pair with no existing bond gets skipped, not a relationship invented from nothing. A survivor with no other mourners produces zero pairs by construction (nobody to catalyze a relationship with), which is correct, not a gap. Rendered visibly in the ships-log under its own "GRIEF — HOW THEY'RE TAKING IT" header, naming both pilots' echoes and the resulting shift, same "visible, not just tracked" rule the tally system already set. Wired into all three loss paths (the sandbox's `triggerLoss()`, the ensemble's `ensTriggerLoss()`/`logKia()`, and the new staged-loss button above). Verified with a targeted suite confirming the echo reads are deterministic off acute pilot state, the delta math matches `ECHO_BOND_LEAN` exactly in both directions, and the lazy-init skip holds.

**Update, 25 Aug 2026 — ✕ Remove now forces a true loss; caught via a real confusion, not a hypothetical.** Maxime tried the "Loss" button above expecting to see a permadeath and kept getting restocks instead — which turned out to be the permadeath rule working exactly as designed (any living Munti anywhere on the roster besides the victim = automatic restock, no roll), just surfaced for the first time by manual testing rather than by RNG luck, on a roster that happened to have a spare Munti. Rather than fight that rule to force a test case, Maxime's own ask: "allow me to trigger permadeath when I click x on the corner and remove the pilot." ✕ Remove's old behavior (silent, no-consequence delete — a pure undo for a mis-drafted pilot) is now a guaranteed true loss: same mourner ripple, muntiesLost tally, and grief catalyst as any other loss, but the permadeath check is skipped entirely rather than rolled, so it always goes through regardless of how many Muntis are on the roster. The shared consequence logic got extracted into `runTrueLoss(victim, cause)` so it's not duplicated between ✕ Remove and Stage Loss's own no-spare-Munti branch. **Net effect: two different buttons for two different jobs.** "Loss" (top-left) still respects the real permadeath rule and can restock — use it to test what an actual random mission loss would look like on this roster. ✕ (top-right) now always goes all the way through — use it when you specifically want to see the true-loss aftermath without fighting the Munti-count mechanic first. **Worth flagging plainly:** this removes the old "just delete a pilot I made by mistake, no fuss" behavior — every removal now fires the full mourning pipeline and a ships-log entry. Not raised as a problem, just noting the tradeoff in case roster-building gets noisy; a quiet-delete affordance can be added back (e.g., a modifier click) if that turns out to be annoying in practice. Verified with a targeted suite: `runTrueLoss()` bypasses the permadeath check even with a spare Munti present (confirmed no save gets credited to the spare, unlike a real restock), the actual ✕ button click drives the same path (not just the underlying function), squad-wide muntiesLost tallying still fires correctly when the victim is a Munti even with another Munti still alive, and Stage Loss's own restock behavior is confirmed unchanged by the refactor.

**Update, 25 Aug 2026 — every true-loss magnitude number doubled.** Maxime, after actually seeing the numbers on screen: "double the effect of permadeath loss across the board. 3-4 point is barely felt." Scoped deliberately to *permadeath specifically* — the restock/close-call branch (someone gets saved) is untouched, same as the drunk debuff (§13.4, a separately-locked number) and the probability gates (`LOSS_CHANCE`, `MUNTI_MISSION_LOSS_CHANCE` — how *often* a loss happens, not how hard it hits once it does). Every number that fires specifically when someone is truly, permanently gone got doubled, in one pass, everywhere it appears:

- **Survivor's acute Stress/Morale hit** (`triggerLoss()` and `ensTriggerLoss()`, identical formulas in both) — Stress base 15–25 → 30–50, closeness multiplier ×0.5 → ×1.0; Morale drop base 12–20 → 24–40, closeness multiplier ×0.35 → ×0.7.
- **Mourner ripple** (shared shape across `triggerLoss()`, `ensTriggerLoss()`, and the standalone `runTrueLoss()` used by ✕ Remove and Stage Loss) — Stress ×0.5 → ×1.0, Morale ×0.35 → ×0.7 of the pair's existing Favorability.
- **muntiesLost Stress toll** (`recordMuntiLost()`, one shared function, so this alone already applied everywhere) — was 2, 4, 6, 8... per repeat loss; now 4, 8, 12, 16...
- **Grief catalyst Favorability shift** (`runGriefCatalyst()`, also one shared function) — scale ×2 → ×4, so a pair's shift now runs roughly −16 to +16 instead of −8 to +8; a single mismatched pair (love+anger, sadness+fear) still nets to 0 by design — that's two echoes canceling out, not the doubling failing to apply.

Verified with a dedicated suite isolating each number and confirming the doubled values land exactly where expected (toll sequence 4/8/12/16, catalyst delta 8 for a sadness+sadness pair that was 4 before, survivor Stress/Morale sampled across 30 trials landing inside the new 30–50 / 24–40 ranges and actually reaching past the old ceilings, mourner ripple exact-matching the new ×1.0/×0.7 math) before shipping.

**Update, 25 Aug 2026 — the design-comparison ask, and two "peripheral" features shipped from it.** Maxime asked "compared to whats out there, what do you think of our social project. what else would you work on" — answered with a comparison against genre peers (Darkest Dungeon 2's Affinity, Rimworld's colony web, XCOM 2's Bonding, Fire Emblem supports, Persona Social Links, Crusader Kings' opinion web), landing on Affinity as the closest analog since it's the only other peer-to-peer, event-driven system with mechanical teeth rather than a single player-facing approval track — worth flagging that this doc's own stated Dragon Age: Origins reference point in §13.2 above is single-track, not peer-to-peer, a real gap between the reference and what's actually been built. Five candidate "peripheral" features were proposed as smaller, self-contained additions that don't touch the still-blocked talk/fight/ambush system (cross-ref below); Maxime approved two — "go for it. have fun." — and asked to leave a third (a capstone/milestone feature) open rather than build it, since he has his own idea for it. Only the two built and the one explicitly parked are recorded here.

**Cross-ref — why this sits below the talk/fight/ambush idea rather than resolving it.** Same thread: "sim talk fight ambish system ca only trully be worked on after the full mission load been confirmed." Confirmed via `project_search` against `Bloom_Wars_Spitball_Ideas.md` that this is a real, recurring idea, not a one-off aside — in-mission social resolution tied to House Amaranth/Colonel Marrow, appearing across roughly 9 of the 36 campaign missions (6, 9, 10, 16, 18, 20, 23, 24, 28). Stays an open question waiting on the mission load; today's two features are deliberately scoped to not depend on it.

**Update, 25 Aug 2026 — Toxic Pairs: bad blood is now a mechanical drag, not just a low number.** Positive Favorability already did real things (dodge/hit bonuses, Stress relief); a deeply negative bond did nothing but sit there. New `TOXIC_THRESHOLD` (-15, chosen to match the UI's own pre-existing "friction" status label, which already called anything below -15 "friction") drives a `toxicFriction()` scale (illustrative only, same footing as every other number in this section: -16 → 2, -50 → ~7, -100 → ~15) applied as a shared Stress tick to both pilots in a toxic pair, any time they're put through a mission, Rec Room activity, or drink together — wired into all three sandbox click handlers and their ensemble-sim equivalents (`ensMissionEvent`/`ensRecEvent`/`ensDrinkEvent`), which now also return the friction value on their event object so `logMissionRecDrink()` can print a "Bad blood:" line alongside the normal event text. A small four-line flavor bank (`TOXIC_LINES`) covers that line's text. Symmetric to the existing close-bond echo bias in `pickEchoForPilot()`: pairs at or below the threshold now lean anger/fear (70/30) with reason "bad blood," the same mechanism the close-bond tier already used for love/warmth. **A boundary bug caught by the test suite and fixed before shipping:** the first pass used `val > TOXIC_THRESHOLD` for the early-return, which treated exactly -15 as already toxic — one point earlier than the UI's own label, which reads -15 as neutral. Corrected to `val >= TOXIC_THRESHOLD` so the mechanic's boundary matches the label it's built to mirror. Verified with a 20-check Playwright suite (threshold boundary in both directions, deep-toxicity scaling, all three activity handlers applying the bite symmetrically to both pilots, non-toxic pairs staying at zero, the echo bias firing with the correct reason string) — all passing after the fix.

**Update, 25 Aug 2026 — Gossip: idle ambient lines now sometimes reference another pilot by name, not just the speaker's own mood.** Previously every idle log line (`logIdleLine()`) was a solo read on the speaker alone (`pickSoloEchoForPilot`). New `findClosestBond()`/`findWorstRival()` scan a pilot's own Favorability pairs for their single best (>=40, matching the existing close-bond bar) or worst (<= `TOXIC_THRESHOLD`) relationship; when either exists, roughly 35% of idle lines now pick from a small warm or toxic flavor bank (`GOSSIP_WARM_LINES`/`GOSSIP_TOXIC_LINES`, 4 lines each, naming the other pilot by an `{o}` placeholder) instead of the plain solo line, via `pickGossipLine()`. Falls back cleanly to the existing plain line whenever a pilot has no qualifying bond in either direction — verified explicitly, not assumed. Reuses `buildDlgLine` unmodified via a synthetic echo object (`{echo: "gossip", reason: ...}`), so it renders through the same dialogue-card path as every other line rather than a parallel rendering system. Verified with the same Playwright suite as Toxic Pairs above (no-bond fallback, correct-bond selection when a barely-positive and an actually-close bond both exist on the same pilot, correct rival naming, and an end-to-end `logIdleLine()` check confirming the rendered log text actually names the other pilot when the gossip roll is forced on).

**Parked, per Maxime's explicit ask — a relationship-progression capstone, not full marriage.** Same thread as the two features above: "keep capstone open I got idea about adding mariange and stuff. maybe not full on mariage but relationship that can lead to kids." Not built, not scoped, not designed beyond this note — recorded here only so it isn't lost before Maxime brings his own version of the idea to the table. Whatever shape it takes, it reads as a capstone on top of the same Favorability spine this whole section already tracks (an existing bond crossing some high bar into a named, milestone-flagged state), rather than a separate system.

**Update, 25 Aug 2026 — Lance assignment, sandbox-only: the roster-grouping half of a bigger ask, deliberately split from the other half.** Grew out of the toxic-pair conversation above — Maxime's own example was Sasuke/Naruto: two rivals who never get put on the same team can still cool from active hostility into hostile-but-fond banter, "frenemy" rather than a permanent grudge, and that needs some notion of "kept apart on purpose" to hang off of. Maxime confirmed the actual unit: **"a lance is 5 man squad[,] 4 random and a munties minimum, can be more munties. but minimum 1."** That's not a new number — it matches the deploy gate Spitball Ideas already documents ("cant go into mission without a munties... squad size 5 in Act I") almost exactly, just phrased per-lance now instead of per-squad.

What's live in the sandbox: every pilot can be assigned to Lance A, B, or C via a click-to-cycle button on their roster card (unassigned → A → B → C → unassigned), capacity-capped at 5 per lance so a lane can never overfill. A summary strip above the roster grid reads each lance's fill (`N/5`) and Munti count, flagging "needs a Munti" until at least one is assigned — advisory only, the sandbox doesn't block an incomplete lance, it just shows the gap. This is pure roster bookkeeping: no mission fires, no Favorability moves, nothing downstream reacts to a lance assignment yet — it only exists so a later mechanic (the frenemy drift proposed alongside Toxic Pairs, still unbuilt, still pending Maxime's go-ahead) has a real "these two are being kept apart" signal to read instead of inventing one. Verified with a 10-check Playwright suite: the full cycle order, capacity capping (a 6th pilot correctly skips a full lane and lands on the next open one), a lance's own current occupant never blocking itself, composition status (`empty` / `building` / `ready`) matching actual membership and Munti count including the full-but-no-Munti case, and the all-three-lanes-full edge case leaving a new pilot correctly unassigned rather than erroring.

**Flagged, not built — the real-engine half of the same ask, and why it's paused here.** Maxime also asked for this "in the game," not just the sandbox, separately noting **"current game dev should be at 15 deployed unit per mission"** — read as three 5-man lances fielded together, 15 total. Checked against the actual engine before writing anything, rather than assuming: `src/scenes/TransporterPad.ts` currently hardcodes `ACT1_DEPLOY_CAP = 5` as a single deploying squad, with its own doc comment explicitly citing "the campaign doc's own §10 squad-scaling table" (5 in Act I, a real 5–8 variable range for Act II, "not built this pass, explicitly out of scope") — which matches Spitball Ideas' own squad-size table exactly, and describes one squad that grows, not three fixed-size lances deployed at once. Nothing currently in the GDD, Data Pack, Spitball Ideas, or the live code describes a simultaneous three-lance, 15-unit deploy — this is new information, not a confirmation of something already locked, and it doesn't obviously square with Mission 1a's own locked briefing line ("five up") or map size (18×12, sized for a small skirmish). Building it for real would touch the deploy-select screen, the mission data schema (`playerPilotIds` would need to become three grouped lists instead of one flat array), the battle scene itself (15 simultaneous player units plus whatever the enemy count scales to), map sizing, AI, and balance — a genuine structural rework, not a peripheral addition, so per the project's own rule this is flagged rather than started. **Worth a direct answer from Maxime:** does 15/three-lanes replace the existing single-squad scaling table outright (meaning Mission 1a's "five up" gets rewritten), or is it a later-Act structure that early missions don't touch? That answer decides whether Spitball Ideas' squad-size table and GDD §10 need updating now. Separately: the Master Index's own doc list still names `combat_sim.py`/`maps.py` as the balance/map validation tools, but neither exists in this session's workspace — the codebase looks to have moved to a TypeScript sim harness (`src/sim/run.ts`) instead, which would need confirming before any real map/balance work on this starts, not assumed.

**Update, 25 Aug 2026 — the question above answered itself the moment the actual campaign doc got read.** Maxime, same day: "its because the number of lance deployed incrase with time as the game goes." Checked against `claude/Bloom_Wars_Independent_Campaign_The_Amaranth_Reckoning.md`, which turns out to already have this exact call locked and dated the same day (§6/§10, self-corrected from an earlier "~20 across 4 lances" draft): Lance A from Mission 1, Lance B integrates at Mission 13 (Warden Company forming), Lance C at Mission 25 — tied to Rourke's own rank progression (2nd Lt. → Capt. → Maj.), not a flat headcount. So "15 deployed" was never quite right as I'd flagged it — the table's own "typical deploy" per act is 5 / 5–8 / 8–12, a partial pull from however many lances exist, not the full lance count fielded at once. 15 is the *battalion's total size* by Act III, not a per-mission deploy number. Worth noting for whenever the real engine work happens, but doesn't block anything below — the sandbox link never needed the tactical deploy-count question resolved, only the lance-integration timing, which is now confirmed straight from source.

**Update, 25 Aug 2026 — the Amaranth Reckoning campaign linked into the sandbox, sim-only, fast-forward pacing.** Maxime: "can you link the campaign over and we can have the simulator go tru it to see how it all goes. mission per mission, optimaly selected lance and all. but this is just for the simulator. not for the live prodect[,] the live product would go tru the relationship at a normal rate, ive got tons of stuff planned for the social part." Same status as every other number in this section — a design/tuning tool, explicitly not a pacing claim, same spirit as the existing Simulate a Day/Week buttons, just walking a real mission list instead of an arbitrary day count.

- **"Load Amaranth Campaign"** resets the roster to Warden Company's five named founders — Rourke "Lark" (Meeps, exempt from permadeath), Bosk "Anvil" (Tank), Iyari "Foxfire" (Meeps/Hiopi), Anand "Farsight" (Reeps/Osnian), Lask "Patch" (Munti) — all seeded into Lance A, with a matching animal-archetype dialogue read on each (wolf/dog/fox/raven/rabbit, chosen off their documented personalities, not new characterization). Roster cards now show a pilot's callsign next to path/species when one's set.
- **All 36 missions are linked by name, act, and objective type**, transcribed straight from the campaign doc's own mission list — only Missions 1-4 have real tactical data built in the actual engine, but the sandbox never simulates combat anyway, so the full 36-entry list (which is all real, confirmed content) is enough to drive the social layer end to end.
- **"Run Next Mission" / "Run Full Campaign (36)"** step through the list. Each mission picks a lance and fires one `ensMissionEvent()` between two of its members — unmodified, so every existing hook (loss rolls, the live permadeath/restock check, grief catalyst, toxic friction, gossip) fires exactly as it would from any other mission event; linking the campaign changes *who* gets paired, never *how* a mission event resolves.
- **Lance B and C aren't named anywhere** — the campaign doc says so outright ("expanded-bench naming is a build-time task, not scoped here") — so the sandbox generates five placeholder recruits for each the moment Mission 13 / Mission 25 is reached, following the same composition rule as manual lance assignment (5 members, ≥1 Munti guaranteed, the other 4 fully random path).
- **"Optimally selected lance," defined and flagged rather than assumed:** the sandbox has no combat model, so there's no win/loss outcome to optimize against. What it optimizes instead is the social read this whole section is about — whichever eligible lance (deploy-gated: needs a living Munti, same rule as the real game) has logged the fewest missions so far gets picked, so exposure and loss risk spread evenly across the battalion instead of one lance racing ahead. A different definition is a one-line change if this isn't the one wanted.
- **The deploy gate is real, not glossed over.** If every currently-active lance has zero living Muntis, that mission is logged as skipped rather than forced through — this sandbox doesn't implement the real design's emergency-Munti-replacement safety net (Amaranth Reckoning §6a), so a genuinely Munti-less run can go quiet for a stretch; the counter still advances so a later-forming lance can pick the campaign back up.
- **Rourke's "no plot armor except the mc" rule is now enforced, not just documented.** `ensTriggerLoss()`'s victim pick now redirects away from any `exemptFromPermadeath` pilot toward whoever they're paired with, so a random mission roll can never actually lose her — matches Maxime's own answer in the campaign doc's §6a ("the only character that is safe is the mc") exactly. Scoped to the ensemble/automatic loss path only (`ensTriggerLoss`, used by `ensMissionEvent`/`ensRecEvent`/`ensDrinkEvent` and now the campaign runner); the manual single-pilot test buttons (Stage Loss, ✕ Remove) are untouched on purpose — those are deliberate, explicit per-pilot actions, a different thing from an automatic roll landing on her by chance.

Verified with a 24-check Playwright suite: campaign load seeds the right five pilots with the right composition and exemption flag, lance eligibility gates correctly by mission number (Lance B/C genuinely unavailable before 13/25), running a mission advances the counter and fires a real event, Lance B/C generate at exactly the right mission with the Munti-minimum rule holding, the "fewest missions logged" rotation actually balances load across lances rather than favoring one, the Munti deploy gate correctly blocks and skips rather than crashing, Rourke survived 0/40 forced-loss rolls where a non-exempt pilot would have been picked, and a full unattended 36-mission run completes cleanly (one seed came out an exactly even 12/12/12 split across the three lances). One bug the suite caught before shipping: `chooseDeployLance()` called a `lanceMuntiCount()` helper that was only ever a comment, never an actual function — fixed to read the Munti count off the already-existing `lanceStatus()` instead.

**Update, 25 Aug 2026 — Command Vacuum: losing a Lance Lead now hits the whole lance, not just their close bonds.** Maxime, approving one of three hook ideas floated off the lance-assignment and Rank & Command work above: "A Lance Lead's Stress bleeding onto their whole lance, not just their close bonds... losing a Lead (or a Lead having a bad day) could reasonably hit the whole 5-person lance harder, since that's what a command vacuum actually feels like. good idea." (The other two floated ideas: a squad-wide combat bonus for a Lead on the field — not addressed, not built; and the observation that Rourke's dual Lance-A-Lead-plus-Company-Commander role would need an exemption from any future frenemy/lance-separation drift mechanic, since she legitimately crosses lance lines on the regular — noted below, not built, since the drift mechanic itself is still unbuilt.)

Two new pieces of state: `state.lanceLeads` (`{A, B, C}`, pilot id or null — one Lead per lance) and a Lead toggle button on each roster card (`☆ Set Lead` / `★ Lead`, shown only once a pilot has a lance assigned; clicking your own current Lead badge again clears it). Cycling a pilot to a different lance (or to unassigned) via the existing lance-cycle button now also clears their old lance's Lead slot if they held it — a Lead's authority doesn't travel with them, and "Lead of a lance you're not even in" would've been a display bug waiting to happen. Loading the Amaranth campaign auto-assigns Rourke as Lance A's Lead from Mission 1, matching her locked role in `Bloom_Wars_Rank_And_Command_v1.md` §3 — she never loses it, including after her Company Commander promotion, since that's additive, not a hand-off.

The mechanic itself: a new shared `applyCommandVacuum(victim)` helper, called from all three true-loss code paths (`runTrueLoss`, `triggerLoss`'s true-loss branch, `ensTriggerLoss`'s kia branch) rather than duplicated a fourth time. Only fires if the victim actually held their lance's Lead slot — losing a non-Lead never touches this. When it fires, every *other* living member of that lance takes a flat Stress +10 / Morale −8 hit (illustrative only, same footing as every other number in this section) and the lance's Lead slot clears (who steps up, if anyone, is an open question per the Rank & Command doc §3/§6 — this sandbox just leaves it vacant rather than guessing a successor). Deliberately squad-wide and NOT Favorability-scaled — same shape as the existing `recordMuntiLost()` tally, not the closeness-gated mourner ripple sitting right next to it in the same functions. A restock (a living Munti saves the Lead from the roll) never triggers this at all, same as it never triggers the mourner ripple — Command Vacuum only fires on an actual, permanent loss.

Verified with a 46-check Playwright suite: the Lead toggle (set, toggle-off, no-op on an unassigned pilot), lance-cycling clearing a vacated Lead slot, `applyCommandVacuum` firing only for an actual Lead and hitting exactly the rest of that lance (a pilot in a different lance stays untouched), all three true-loss call sites integrating the ripple into their respective log/status messages, the restock branch never firing it, campaign load auto-assigning Rourke, and the actual DOM button rendering and click-handling correctly. One test-design false alarm caught and fixed before trusting the suite: an early version of the restock-path check used the pair's own survivor as the "should stay untouched" witness, which coincidentally overlapped the restock formula's own legitimate Stress range (5–10) with Command Vacuum's flat 10 — reworked to use an uninvolved third lance-mate instead, since the survivor's Stress change there is expected and unrelated to this mechanic.

**Design note, not built — Rourke's frenemy exemption.** Recorded here so it isn't lost before the frenemy/lance-separation drift mechanic (proposed alongside Toxic Pairs above, still unbuilt) gets designed for real: whenever that mechanic exists, Rourke should be exempt from it, mirroring her existing permadeath exemption. She's the one pilot who legitimately crosses lance lines on the regular — commanding B and C while still embedded in A — which cuts against "different lance = kept apart, drifts toward frenemy" unless she's deliberately carved out.

**Parked, per Maxime — the MC as a player-customizable character.** Same thread, responding to the note above about Rourke being a genuine exception to lance-separation logic: "she's the MC. eventually we will have to make the MC something a player can modify to preference. but this is fine as is [for now]." Not scoped, not designed — Rourke stays a fixed, authored character everywhere in this sandbox and the real campaign data for now. Recorded only so the eventual player-customizable-MC idea isn't lost, same treatment as the marriage/kids capstone note above.

**Bug found and fixed, 25 Aug 2026 — the permadeath exemption wasn't actually consistent across every roll.** Maxime, reacting to the Command Vacuum work above: "the only thing the MC is exempt is [death itself, the] canned answer[.] i think she should feel loss and panic too." That second half was already true and stayed true — nothing in the exemption logic has ever gated Stress, Morale, the mourner ripple, the muntiesLost tally, or Command Vacuum; `exemptFromPermadeath` is checked in exactly one place in the whole file. But confirming that surfaced a real gap in the first half: `ensTriggerLoss()` (the ensemble/campaign path) had the victim/survivor redirect that makes the exemption actually work; `triggerLoss()` (the sandbox's own direct pair-slot path, fired by `LOSS_CHANCE`/`MUNTI_MISSION_LOSS_CHANCE` random rolls on the Simulate Mission button) never did. An exempt pilot sitting in the sandbox's own pair slots could, until now, actually be rolled as the true-loss victim there and die outright — a real inconsistency against the locked rule, not a hypothetical.

Fixed by porting the same swap into `triggerLoss()`, with one deliberate carve-out: `triggerLoss()` is also the function `stagePilotLoss()` routes into when the "Loss" button is clicked on a pilot already sitting in the pair slots, and that's a deliberate, named-pilot test action, not a random roll — same category as ✕ Remove bypassing the permadeath check entirely. Redirecting a click aimed straight at Rourke onto her partner instead, without the person clicking Loss on her ever choosing that, would be a surprising thing for a test button to do. New `deliberate` param on `triggerLoss(forcedVictim, deliberate)`, set true only by `stagePilotLoss()`'s call; the two automatic roll call sites are unchanged and now correctly redirect. Verified with a 9-check suite (40 forced-victim trials confirming she's never the one lost via an automatic roll, a no-op-swap case confirming she still takes the acute survivor Stress/Morale hit when she's already the non-victim, the deliberate Stage-Loss case confirming a direct click on her tests her specifically, an `ensTriggerLoss()` regression check, and the restock branch untouched) plus a separate 4-check interaction suite confirming the redirect resolves *before* Command Vacuum evaluates — an exempt pilot who's also her lance's Lead correctly never triggers her own lance's vacuum, since the swap means she never actually dies in the first place, while a non-exempt Lead's real loss still fires it normally.

**Design correction surfaced, not yet built, 25 Aug 2026 — the swap itself is the wrong shape now that the real mechanism is known.** Maxime, cutting straight past the "she's exempt" framing to why: "the mc only has plot armor becasuse if she dies the missions failed and its back to mission briefing." That's a materially different mechanism than what's built here. The swap logic above treats her exemption as "the death still happens, just to someone else" — which means every time an automatic roll lands on her, the sandbox invents a death for her partner that the real design never asked for. The actual rule is closer to a protect-the-VIP objective: if she'd go down, nothing resolves at all — the mission attempt fails and the player retries from briefing, so nobody's roster changes, nobody's Stress/Morale moves, no Command Vacuum fires, nothing. Full reasoning recorded in `Bloom_Wars_Independent_Campaign_The_Amaranth_Reckoning.md` §6a rather than duplicated here — that's now the doc of record for *why*; this entry is the *where it lives in code* side.

**Why this isn't a quick swap-out, checked before touching anything.** Replacing the redirect with a true no-op is straightforward in `triggerLoss()` itself (skip the swap, skip the permadeath check entirely, log a distinct "mission fails, command pulls back" line, return without touching `state.roster`/Stress/Morale). It's a bigger lift in `ensTriggerLoss()`, because that function's return value is a typed event object (`kia` / `restock`) that every caller — `simulateDay()`'s dispatcher, and the equivalent one driving the linked-campaign runner — switches on to decide which render function to call (`logKia`/`logRestock`/`logMissionRecDrink`, the last of which assumes `evt.a`/`evt.b` and would break on a bare no-op event). A clean fix needs a real third branch wired through both dispatchers, not just a return-early inside `ensTriggerLoss()` itself — checked the callers directly rather than assuming this was a one-line change. Flagging this rather than building it this pass, since it's a genuine (if small) scope question — what the no-op event should actually look like in the log (silent, or a one-line "command recalls the mission" flavor beat) is worth a real answer from Maxime before it's built, not guessed at here.

### 13.3 Likely overlap with Romance — flagged, not merged

Spitball Ideas' romance entry left "what it actually does mechanically once formed" as an open question (a Stress bonus, an in-combat synergy buff, or purely narrative). Favorability now answers a version of that question for *every* relationship, romantic or not — which raises the obvious question neither conversation has settled yet: is romance its own separate track, or is it Favorability with a high-favorability pilot plus a flag/scene layered on top (the same "close friend / bromance" ceiling Hiopi and Carabil cap out at, just with a romance option unlocked past it for everyone else)? The second reading is cheaper to build and keeps one number per relationship instead of two, and it's consistent with how the "close friend / bromance" tier was already described as a real *ceiling*, implying romance is a *further* tier on the same scale rather than a parallel one. Not deciding this here — flagging it because building Favorability first and romance second, without checking whether they're the same axis, risks building two systems that should've been one.

### 13.4 The universal hit/miss roll — proposed, flagged, and set aside (added 23 Aug 2026)

Maxime's original message also asked, separately from the hub work above and ahead of more enemy content: "lets add in the infamous 98% chance to hit and still miss dc rolls." Flagged back before building anything, since it isn't a small add — every attack in the engine resolves deterministically today (`engine/combat.ts`, damage vs. defense, no accuracy check anywhere), and the whole existing balance pass (the damage cap of 90, the class-triangle's 20-35 margins, all 296 tests) was tuned assuming that holds. A universal accuracy roll changes the resolution model itself, not just one more verb like Overwatch or Sensor Sweep were.

**Resolved, 23 Aug 2026 — parked, not built.** Maxime, after hearing the tradeoff: "just try it out. but your right it doesnt make sense." Read as agreement, not a build order — the universal system doesn't get built. **Not the same as walking back the two things it was in service of, though — Maxime, same breath: "but the buff to moral and debuff on drunk are good to keep."** Both stay real design intent:

- **Favorability's morale/Stress relief** — untouched by this, never depended on a hit-chance system in the first place (§13.2 above).
- **The drunk debuff** — restated with real numbers this time, see §13.2: −20% hit chance, a few turns. This is exactly the kind of narrow, scoped effect that doesn't need the shelved universal system to exist — it can be built later as its own status-effect check (does this unit currently have the "drunk" flag? if so, apply a flat accuracy malus to its own attacks) without touching how every other attack in the game resolves. Worth remembering when this eventually gets built: implement the narrow version, don't back into the universal system by accident while building it.
- **Favorability's own "better hit chance" bonus** (§13.2) — genuinely more open now than the drunk debuff. A scoped "this specific bonus improves this specific roll" only makes sense once there's a roll to improve; without the universal system, this bullet stays intent-only until either a similarly narrow mechanism is designed for it specifically, or the universal system gets revisited later for real (not ruled out forever — just not now, and not blocking anything else in this doc).

Nothing here is built. Same status as the rest of §13 — design only, and now also explicitly not queued for the next content pass, per Maxime's own call.
